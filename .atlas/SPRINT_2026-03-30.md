# Sprint Plan — 2026-03-30 Batch

> Batched by dependency and logical grouping. Each batch targets a coherent slice of work.

---

## Batch 1 — Settings & UI Controls (Quick Wins)

All are UI-only changes in the settings panel area — no game logic changes. Can be done quickly in sequence.

| ID | Item | Type | Risk |
|----|------|------|------|
| F-164 | Remove Basic/Advanced/Developer settings toggle — show all sections by default | Feature | Low |
| B-50 | Restore arrow appearance sliders to Map & Grid section | Bug | Low |
| F-167 | Add per-element player toggles for star labels/ship counts | Feature | Low |

**Files likely touched:** `GameSettingsPanel.svelte`, `ControlsSection-*.svelte`

---

## Batch 2 — Order Arrow Visual Overhaul

These three items are all about the order arrows rendering pipeline — do them together.

| ID | Item | Type | Risk |
|----|------|------|------|
| B-55 | Fix arrowhead opacity to match shaft; make independently adjustable | Bug | Low |
| B-56 | Fix damaged ship size to obey shipSize controls | Bug | Low |
| F-166 | Add outline option to arrows + new "triangular ownership pull" style where ownership ring deforms into a point along order lane | Feature | Medium |

**Files likely touched:** `GameCanvas.svelte` (arrow rendering), `fxOrchestrator`, ship rendering logic

---

## Batch 3 — Map Management & Load/Save

All relate to how maps are organized, loaded, and selected. Tightly coupled.

| ID | Item | Type | Risk |
|----|------|------|------|
| B-51 | Separate saved maps from classic maps — classics are read-only, separate list | Bug | Low |
| B-52 | Add actual file load/import option to Load Game in settings | Bug | Low |
| B-53 | MP map selection — allow choosing map type when creating MP room (not just random) | Bug | Medium |

**Files likely touched:** `MainMenu.svelte`, `GameContainer.svelte`, `gameStore.svelte.ts`, `multiplayerStore.svelte.ts`, `GameRoom.ts`

---

## Batch 4 — Game Flow & Restart

Restart is broken and may be entangled with audio. Investigate together.

| ID | Item | Type | Risk |
|----|------|------|------|
| B-54 | Restart does not actually restart | Bug | High |
| — | Investigate `AudioManager` `NotSupportedError` — may be cause or side-effect | Debug | Medium |

**Files likely touched:** `gameStore.svelte.ts` (restart flow), `AudioManager.ts`, `GameContainer.svelte`

---

## Batch 5 — Territory Tuning

DX tuning is independent and can be done standalone.

| ID | Item | Type | Risk |
|----|------|------|------|
| F-170 | DX equal split — both intervening players should split the gap equally instead of one dominating | Tuning | Medium |

**Files likely touched:** `geometryModifiers.ts`, PVV compiler, territory geometry pipeline

---

## Batch 6 — Main Menu UX Enhancement

Larger UX feature; lower urgency but high polish value.

| ID | Item | Type | Risk |
|----|------|------|------|
| F-168 | Random map preview + reshuffle button in Main Menu before starting game | Feature | Medium |

**Files likely touched:** `MainMenu.svelte`, `mapgen` (preview render), possibly a new `MapPreview.svelte` component

---

## Batch 7 — Renderer Work (Larger Scope)

PVV2 revival is a significant engineering task. Separate from everything else.

| ID | Item | Type | Risk |
|----|------|------|------|
| F-169 | Bring PVV2 renderer into modern master and get it working | Feature | High |

**Files likely touched:** `PowerVoronoiRenderer_DY4.ts`, `GameCanvas.svelte` (renderer dispatch), territory config

---

## ✅ Already Completed

| ID | Item | Status |
|----|------|--------|
| F-165 | Custom Beehiiv mailing list signup form (Hero + Footer) | ✅ Done |
| F-171 | Discord community link on landing page | ✅ Done |
| — | Fatherlion Studios image in DeveloperVision | ✅ Done |

---

## Recommended Execution Order

```
Batch 1 → Batch 2 → Batch 4 → Batch 3 → Batch 5 → Batch 6 → Batch 7
```

**Rationale:** Start with quick UI wins (Batch 1) for momentum, then fix arrow visuals (Batch 2) since they're related. Restart bug (Batch 4) is high-priority usability. Map management (Batch 3) is a medium-effort coherent block. Territory tuning (Batch 5) is standalone. Map preview (Batch 6) and PVV2 revival (Batch 7) are larger features that can be deferred.
