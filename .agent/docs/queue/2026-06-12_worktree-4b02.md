# 2026-06-12 Queue - worktree 4b02

## Completed

- Imported external Aurelia Drift HUD package into `pax-fluxia/src/lib/aurelia-hud/`.
- Added `/dev/aurelia-hud` route.
- Added live topbar link to the imported HUD demo.
- Added `@ark-ui/svelte` dependency.
- Updated HUD handoff with merge-specific implementation notes.
- Added first design-system foundation: Pax theme tokens, theme metadata, Tailwind Variants HUD recipes, theme root hook, and legacy `--hud-*` token aliases.
- Added Appearance-panel HUD Theme selector using shared `paxThemeState` and persisted `pax-ui-theme-id`.
- Migrated `HudPanel` and `HudIconButton` to consume Tailwind Variants recipes while preserving `pf-*` compatibility classes.
- Migrated `HudRail` and `GameSpeedPanel` button styling to design-system recipes.
- Enabled Svelte Inspector dev tooling through `pax-fluxia/svelte.config.js`.
- Added Pax HUD primitive component base in `pax-fluxia/src/lib/design-system/components/`.
- Encapsulated Ark Tooltip/ToggleGroup behavior inside Pax primitives.
- Extended Tailwind Variants HUD recipes for tooltip, segmented control, field, and range primitives.
- Migrated live HUD/settings target components to consume `PaxHud*` primitives instead of raw controls, direct Ark imports, or direct variant recipe calls.
- Validated the primitive migration with production build, `git diff --check`, and a static audit for disallowed live-HUD control patterns.
- Reduced dark-alpha stops in the shared Pax border gradients.
- Rebuilt `pax-fluxia/src/lib/components/ui/settings/panel-shared.css` as a tokenized rounded Aurelia Drift bridge for legacy settings/tuning panels.
- Validated the shared settings grammar with production build and `git diff --check`.
- Added shared settings drawer/info-row primitives in `pax-fluxia/src/lib/design-system/components/`.
- Migrated the active Settings rail tool panes for Theme Library, Appearance, Stats, Hotkeys, and Help from local panel markup to those primitives.
- Removed the old local Stats/Help/tool-panel CSS hooks from `GameSettingsPanel.svelte`.
- Validated the drawer migration with production build, `git diff --check`, and a static live-HUD audit for raw controls/direct Ark/direct recipe calls.
- Added shared settings range/toggle row primitives.
- Migrated AI tuning ranges, Logging toggles/actions, and the reusable `SliderRow.svelte` wrapper to consume those primitives.
- Validated the settings control migration with production build, `git diff --check`, and targeted raw-control audit.
- Rebuilt `PerimeterFieldTuning.svelte` around Pax button/select/range/toggle primitives while preserving existing config write paths.
- Removed the `PerimeterFieldTuning.svelte` unused `.sub-heading` warning by removing the obsolete local selector and raw local markup.
- Removed obsolete local CSS-only selectors from `ControlsSection-Territory.svelte` that had no live markup owner.
- Confirmed the Territory/Perimeter CSS warning set no longer appears in production build output.

## Remaining

- Continue the live HUD redesign through the Pax primitive/token system; do not add isolated visual patches.
- Continue migrating stacked Settings section panels and high-traffic settings/tuning internals from legacy local controls to `src/lib/design-system/components/`.
- Continue replacing high-traffic legacy settings controls with Pax primitives; the previous Territory/Perimeter unused-selector warning set is cleared.
- Push the Aurelia Drift visual polish from tokens/primitives after the component base owns the relevant surface.
- Bind Aurelia HUD package view models to real game state if this package becomes the live HUD target.
- Replace package demo state and fake concepts before using it in production.
- Self-host `Cinzel` and `Rajdhani` for offline/Tauri/Steam packaging.
- Visual QA the route manually, per user preference.
