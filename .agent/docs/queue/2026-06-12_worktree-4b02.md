# 2026-06-12 Queue - worktree 4b02

## Completed

- Imported external Aurelia Drift HUD package into `pax-fluxia/src/lib/aurelia-hud/`.
- Added `/dev/aurelia-hud` route.
- Added live topbar link to the imported HUD demo.
- Added `@ark-ui/svelte` dependency.
- Updated HUD handoff with merge-specific implementation notes.
- Added first design-system foundation: Pax theme tokens, theme metadata, Tailwind Variants HUD recipes, theme root hook, and legacy `--hud-*` token aliases.
- Added Appearance-panel HUD Theme selector using shared `paxThemeState` and persisted `pax-ui-theme-id`.
- Migrated `HudPanel` and `HudIconButton` to consume Tailwind Variants recipes while preserving `pf-*` compatibility classes.
- Migrated `HudRail` and `GameSpeedPanel` button styling to design-system recipes.
- Enabled Svelte Inspector dev tooling through `pax-fluxia/svelte.config.js`.
- Added Pax HUD primitive component base in `pax-fluxia/src/lib/design-system/components/`.
- Encapsulated Ark Tooltip/ToggleGroup behavior inside Pax primitives.
- Extended Tailwind Variants HUD recipes for tooltip, segmented control, field, and range primitives.
- Migrated live HUD/settings target components to consume `PaxHud*` primitives instead of raw controls, direct Ark imports, or direct variant recipe calls.
- Validated the primitive migration with production build, `git diff --check`, and a static audit for disallowed live-HUD control patterns.
- Reduced dark-alpha stops in the shared Pax border gradients.
- Rebuilt `pax-fluxia/src/lib/components/ui/settings/panel-shared.css` as a tokenized rounded Aurelia Drift bridge for legacy settings/tuning panels.
- Validated the shared settings grammar with production build and `git diff --check`.
- Added shared settings drawer/info-row primitives in `pax-fluxia/src/lib/design-system/components/`.
- Migrated the active Settings rail tool panes for Theme Library, Appearance, Stats, Hotkeys, and Help from local panel markup to those primitives.
- Removed the old local Stats/Help/tool-panel CSS hooks from `GameSettingsPanel.svelte`.
- Validated the drawer migration with production build, `git diff --check`, and a static live-HUD audit for raw controls/direct Ark/direct recipe calls.
- Added shared settings range/toggle row primitives.
- Migrated AI tuning ranges, Logging toggles/actions, and the reusable `SliderRow.svelte` wrapper to consume those primitives.
- Validated the settings control migration with production build, `git diff --check`, and targeted raw-control audit.
- Rebuilt `PerimeterFieldTuning.svelte` around Pax button/select/range/toggle primitives while preserving existing config write paths.
- Removed the `PerimeterFieldTuning.svelte` unused `.sub-heading` warning by removing the obsolete local selector and raw local markup.
- Removed obsolete local CSS-only selectors from `ControlsSection-Territory.svelte` that had no live markup owner.
- Confirmed the Territory/Perimeter CSS warning set no longer appears in production build output.
- Added `pixi.js` to client Vite `optimizeDeps.include` and removed stale `@colyseus/schema` from that client optimize list to address the reported Pixi environment handler import failure during live game shell loading.
- Validated the Pixi optimize fix with forced Vite optimization and production build.
- Added `PaxHudFileButton` as the design-system file-picker primitive.
- Rebuilt shared `CategoryThemeBar.svelte` around Pax button/icon/select/text/file primitives and project logger.
- Validated Category Theme Bar migration with production build, `git diff --check`, and targeted raw visible-control audit.
- Rebuilt `ControlsSection-Visuals.svelte` around Pax button/select/segmented/range/toggle primitives while preserving existing visual config write paths.
- Validated Visuals settings migration with production build, `git diff --check`, and targeted raw visible-control audit.
- Extended `PaxSettingsRangeRow` with custom output text for values like `unlimited`.
- Migrated Combat/Battle and Economy settings ranges to `PaxSettingsRangeRow`.
- Validated Combat/Economy migration with production build, `git diff --check`, and targeted raw-control audit.
- Rewrote Travel and Conquest settings surfaces around `PaxHudSelect`, `PaxSettingsRangeRow`, and `PaxSettingsToggleRow`.
- Validated Travel/Conquest migration with production build, `git diff --check`, and targeted raw-control audit.
- Added `PaxSettingsPickerRow` as a reusable settings option-menu primitive with HudIcon-backed chevrons and preview action.
- Rewrote Audio settings around Pax button/range/toggle/picker primitives while preserving audio manager config writes, sound preview, file selection, offsets, and conquest routing behavior.
- Validated Audio migration with production build, `git diff --check`, feature-level raw-control audit, and picker glyph cleanup audit.
- Migrated mobile live HUD `SpeedControls.svelte` to `PaxHudButton` and fixed the recurring Svelte prop-state warning.
- Validated SpeedControls migration with production build, `git diff --check`, and targeted raw-control/state-warning audit.
- Rewrote Timing settings around Pax range/toggle/button primitives, including the territory-transition lock controls.
- Validated Timing migration with production build, `git diff --check`, and targeted raw-control/legacy-class audit.
- Rewrote Surge settings around `PaxSettingsRangeRow` and `PaxSettingsToggleRow`.
- Validated Surge migration with production build, `git diff --check`, and targeted raw-control/legacy-class audit.
- Rewrote Frontier FX settings around `PaxHudSelect`, `PaxSettingsRangeRow`, and `PaxSettingsToggleRow`.
- Validated Frontier FX migration with production build, `git diff --check`, and targeted raw-control/legacy-class audit.
- Added disabled-state support to `PaxHudSelect`.
- Migrated small settings utility surfaces to Pax primitives: Geometry Source, Settings Dump Diagnostics, Perf Scenario Runner, Perimeter Field Diagnostics, and Territory SLA widget.
- Validated the small utility batch with production build, `git diff --check`, and targeted raw-control/legacy-class audit.
- Added reusable `PaxColorSwatchButton` for themeable color choices.
- Migrated Players palette, Territory Transition tuning, and Territory Topology tuning to Pax primitives while preserving palette/config write paths.
- Validated the Players/Transition/Topology batch with production build and targeted raw-control/legacy-class audit.
- Migrated Diagnostics and Territory Engine Trace diagnostics controls to Pax primitives while preserving overlay, ruler, recorder, export, and trace-step behavior.
- Validated the Diagnostics batch with production build and targeted raw-control/legacy-class audit.
- Migrated `TerritorySurfaceStyleTuning.svelte` surface controls to Pax select/range/toggle primitives while preserving fill, border, Ember Lattice, and finish config writes.
- Validated the Territory Surface Style migration with production build and targeted raw-control/legacy-class audit.
- Replaced `ThemeSelectDropdown.svelte` custom raw-button listbox with `PaxSettingsPickerRow` while preserving the public props and current no-group-label usage.
- Validated the Theme Select Dropdown migration with production build and targeted raw-control audit.
- Migrated `MetaballGridTuning.svelte` module visibility and frontier preset buttons to Pax segmented/button primitives.
- Validated the Metaball button/segmented-control slice with production build and targeted raw-button audit.
- Migrated the remaining `MetaballGridTuning.svelte` grid, border, frontier, wave, flip, and Phase Field finish-tail controls to Pax select/range/toggle primitives while preserving existing `writeConfig(...)` keys.
- Validated the full Metaball control slice with production build, `git diff --check`, and a targeted audit showing no raw `<button>`, `<select>`, `<input>`, inline `style=`, or active class toggles remain in `MetaballGridTuning.svelte`.
- Began `ControlsSection-Ships.svelte` migration by extracting shared config write helpers and moving Star System Scale plus Ship Size/Shape controls to Pax range/toggle primitives.
- Validated the Ships size/shape slice with production build, `git diff --check`, and targeted raw-control count reduction.
- Migrated `ControlsSection-Ships.svelte` Star Halos controls to Pax range/toggle/button/segmented primitives while preserving halo preset and fleet mode config writes.
- Validated the Ships Star Halos slice with production build, `git diff --check`, and targeted raw-control count reduction.
- Migrated `ControlsSection-Ships.svelte` Orbit Layout range controls to Pax range primitives while preserving orbit/star radius config writes.
- Validated the Ships Orbit Layout slice with production build, `git diff --check`, and targeted raw-control count reduction.
- Migrated `ControlsSection-Ships.svelte` Star Shape and Ownership Ring controls to Pax segmented/range primitives while preserving star-shape and ownership-ring config writes.
- Validated the Ships Star Shape/Ownership Ring slice with production build, `git diff --check`, and targeted raw-control count reduction from `83` to `70`.
- Migrated `ControlsSection-Ships.svelte` Star Labels controls to Pax segmented/range/toggle primitives while preserving label layout, scale cascade, color, opacity, border, and leash config writes.
- Validated the Ships Star Labels slice with production build, `git diff --check`, and targeted raw-control count reduction from `70` to `35`.
- Completed the remaining `ControlsSection-Ships.svelte` migration by moving Order Arrows, Damaged Ships, Interaction, Density Coloring, and Star Glow controls to Pax primitives.
- Validated the completed Ships migration with production build, `git diff --check`, and a targeted audit showing zero raw visible controls/inline styles/active-class toggles in `ControlsSection-Ships.svelte`.
- Fixed a callback contract regression by updating `PaxSettingsToggleRow.svelte` to support both `onChange` and `onToggle`; documented the mistake in `.agent/docs/project/post-mortems/2026-06-12_pax-settings-toggle-row-callback-alias.md`.
- Began `ControlsSection-Territory.svelte` migration by moving the system/renderer module selectors, render-mode selector, deprecated-mode actions, and transition select to Pax primitives.
- Validated the Territory navigation slice with production build, `git diff --check`, and targeted raw-control/style audit reduction from `79` to `56`.
- Migrated the `ControlsSection-Territory.svelte` Metaball CPU-grid core controls to Pax primitives: cell size, influence radius, falloff select, dominance threshold, geometry-follow toggle, strength multiplier, coverage padding, and blend sharpness.
- Restored a missing `TerritorySurfaceStyleTuning` component tag in the Metaball section; the prior markup had orphaned props and did not render the shared surface style controls there.
- Validated the Territory Metaball CPU-grid slice with production build, `git diff --check`, and targeted raw-control/style audit reduction from `56` to `47`.
- Completed the remaining `ControlsSection-Territory.svelte` migration by moving Combat/Fleet Pressure, Frontier Topology, Engine Surface shape/motion, runtime fill/border, and remaining helper-copy rows to Pax primitives/classes.
- Validated the completed Territory migration with production build, `git diff --check`, and a targeted zero-match raw-control/style audit for `<button>`, `<select>`, `<input>`, inline `style=`, `class:active`, and `class:is-active`.
- Added an `accentId` prop to `PaxHudButton` and moved `GameSettingsPanel.svelte` tool/section accents from inline styles to data-driven CSS selectors.
- Validated `GameSettingsPanel.svelte` with a zero-match raw-control/inline-style audit and production build.

## Remaining

- Continue the live HUD redesign through the Pax primitive/token system; do not add isolated visual patches.
- Continue migrating stacked Settings section panels and high-traffic settings/tuning internals from legacy local controls to `src/lib/design-system/components/`; Category Theme Bar, Visuals, Combat/Battle, Economy, Travel, Conquest, Audio, Timing, Surge, Frontier FX, Players, Diagnostics, Territory Surface Style, Territory Transition/Topology, small diagnostics/utilities, AI, Logging, Perimeter, and shared top-level drawers now have examples to follow.
- `GameSettingsPanel.svelte`, `ControlsSection-Ships.svelte`, and `ControlsSection-Territory.svelte` now have zero-match raw-control/style audits.
- Continue replacing high-traffic legacy settings controls with Pax primitives; the previous Territory/Perimeter unused-selector warning set is cleared.
- Remaining production build warnings after the SpeedControls fix are chunk-size and dynamic/static import warnings, not touched-file Svelte warnings.
- Push the Aurelia Drift visual polish from tokens/primitives after the component base owns the relevant surface.
- Bind Aurelia HUD package view models to real game state if this package becomes the live HUD target.
- Replace package demo state and fake concepts before using it in production.
- Self-host `Cinzel` and `Rajdhani` for offline/Tauri/Steam packaging.
- Visual QA the route manually, per user preference.
