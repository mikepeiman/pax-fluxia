# Engine Architecture ΓÇö Current State  **Last Updated:** 2026-02-12   **Purpose:** Document where game logic currently lives across all three packages  ---  ## Architecture Overview  ```mermaid graph TB     subgraph common["@pax/common (shared, stateless)"]         CE["GameEngine.tick(state, config)"]         CC["calculateCombat()"]         CQ["applyConquest()"]         CP["applyProduction()"]         CR["applyRepair()"]         CO["calculateTransfer()"]         CF["EngineConfig + DEFAULT_ENGINE_CONFIG"]         CS["STAR_TYPE_STATS"]     end      subgraph client["pax-fluxia (client, stateful SP)"]         CLE["GameEngine.ts (1594 lines)"]         CLS["Star.ts (340 lines)"]         CLC["Combat.ts ΓÇö calculateCombatV4()"]         CLA["AI.ts (configurable)"]         CLH["HexGrid.ts (Delaunay map gen)"]         CLG["GAME_CONFIG (mutable, localStorage)"]     end      subgraph server["pax-server (Colyseus, MP)"]         SE["GameRoom.ts (700 lines)"]         SA["processAI() ΓÇö inline 50-line heuristic"]         SM["initStandardMap() ΓÇö random scatter"]     end      %% Server uses shared     SE -->|"delegates tick"| CE     CE --> CC     CE --> CQ     CE --> CP     CE --> CR      %% Client duplicates     CLE -->|"own combat"| CLC     CLE -->|"delegates conquest"| CQ     CLS -->|"delegates production"| CP     CLS -->|"delegates repair"| CR     CLE -->|"reads"| CLG      style CLC fill:#ff6b6b,stroke:#333,color:#000     style CLA fill:#ffd93d,stroke:#333,color:#000     style SA fill:#ffd93d,stroke:#333,color:#000     style SM fill:#ff6b6b,stroke:#333,color:#000     style CLH fill:#4ecdc4,stroke:#333,color:#000 ```  **Legend**: ≡ƒö┤ Red = Duplicated/conflicting, ≡ƒƒí Yellow = Different implementations, ≡ƒƒó Teal = Unique (should be shared)  ---  ## Duplication Matrix  | Feature | Common | Client | Server | Status | |---------|--------|--------|--------|--------| | **Combat formula** | `calculateCombat()` | `calculateCombatV4()` (wrapper ΓåÆ Common) | via Common | Γ£à **Unified** (thin wrapper remains) | | **Transfer rate** | `EngineConfig.TRANSFER_RATE = 0.1` | `GAME_CONFIG.TRANSFER_RATE` | via Common | ΓÜá∩╕Å **3 sources** (also `ORDER_CONFIG = 0.25`) | | **Tick orchestration** | `GameEngine.tick()` | Own `executeTick()` | Calls `GameEngine.tick()` | ΓÜá∩╕Å **Client reimplements** | | **Combat resolution** | `resolveMultiSourceCombat()` | Own `resolveMultiSourceCombat()` + `resolveCombat()` | via Common | ΓÜá∩╕Å **Client reimplements** | | **Transfer processing** | `processOrders()` | Own `executeTransferOrders()` | via Common | ΓÜá∩╕Å **Client reimplements** | | **Win condition** | Star count only | Star count + 99% dominance | via Common | ΓÜá∩╕Å **Client is richer** | | **Map generation** | Γ¥î | Hex grid + Delaunay | Random scatter | ΓÜá∩╕Å **Two algorithms** | | **AI** | Γ¥î | Configurable `AI.ts` | 50-line inline heuristic | ΓÜá∩╕Å **Two implementations** | | Production | `applyProduction()` | `Star.produce()` ΓåÆ delegates | via Common | Γ£à Unified | | Repair | `applyRepair()` | `Star.repair()` ΓåÆ delegates | via Common | Γ£à Unified | | Conquest | `applyConquest()` | Delegates + logging wrapper | via Common | Γ£à Mostly unified | | Star type stats | `STAR_TYPE_STATS` | Reads from Common | Reads from Common | Γ£à Unified |  ---  ## Data Flow ΓÇö Single Player  ``` GAME_CONFIG (mutable, localStorage)     Γåô reads Client GameEngine.executeTick()     Γö£ΓöÇΓöÇ executeTransferOrders()     ΓåÉ duplicated from Common     Γöé   ΓööΓöÇΓöÇ calculates transfer amount using GAME_CONFIG.TRANSFER_RATE     Γö£ΓöÇΓöÇ resolveMultiSourceCombat()  ΓåÉ duplicated from Common     Γöé   ΓööΓöÇΓöÇ calls calculateCombatV4()  ΓåÉ NOT Common's calculateCombat()     Γöé   ΓööΓöÇΓöÇ calls executeConquest()     Γöé       ΓööΓöÇΓöÇ delegates to applyConquest()  Γ£à shared     Γö£ΓöÇΓöÇ production                  Γ£à Star.produce() ΓåÆ applyProduction()     Γö£ΓöÇΓöÇ repair                      Γ£à Star.repair() ΓåÆ applyRepair()     Γö£ΓöÇΓöÇ AI.evaluate()               ΓåÉ client-only configurable AI     Γö£ΓöÇΓöÇ checkWinCondition()         ΓåÉ richer than Common (99% dominance)     ΓööΓöÇΓöÇ emits TickEvents ΓåÆ activeGameStore ΓåÆ GameCanvas ```  ## Data Flow ΓÇö Multiplayer  ``` Client: gameplayConfig from GAME_CONFIG ΓåÆ sent to server at room creation     Γåô Server: GameRoom.executeTick()     Γö£ΓöÇΓöÇ processAI()                ΓåÉ inline 50-line heuristic (weaker than client AI)     Γö£ΓöÇΓöÇ GameEngine.tick(state, engineConfig)  Γ£à shared     Γöé   Γö£ΓöÇΓöÇ processProduction()    Γ£à shared     Γöé   Γö£ΓöÇΓöÇ processOrders()        Γ£à shared (uses calculateCombat, NOT calculateCombatV4)     Γöé   Γö£ΓöÇΓöÇ processRepair()        Γ£à shared     Γöé   ΓööΓöÇΓöÇ checkWinCondition()    Γ£à shared (but simpler)     ΓööΓöÇΓöÇ broadcasts TickEvents to clients ```  ---  ## Three Transfer Rates  | Source | Value | Used By | |--------|-------|---------| | `GAME_CONFIG.TRANSFER_RATE` | User-set (default 10) | Client SP engine | | `DEFAULT_ENGINE_CONFIG.TRANSFER_RATE` | `0.1` | Server (via Common) | | `ORDER_CONFIG.TRANSFER_RATE` | `0.25` | `calculateTransfer()` in `orders.ts` |  Only `GAME_CONFIG.TRANSFER_RATE` is connected to the UI slider. The user sets it to (e.g.) 10, which becomes 0.10. The Common engine uses `DEFAULT_ENGINE_CONFIG` which defaults to 0.1. `ORDER_CONFIG` at 0.25 is a stale orphan likely never called.  ---  ## Combat Formula Comparison  | Aspect | `calculateCombatV4` (Client) | `calculateCombat` (Common) | |--------|------------------------------|---------------------------| | File | `pax-fluxia/src/lib/config/game.config.ts` | `common/src/combat.ts` | | Used by | Client SP engine | Server via Common engine | | Implementation | **Thin wrapper** ΓÇö calls `calculateCombat()` | The actual formula | | Config source | `GAME_CONFIG` slider values as `configOverrides` | `COMBAT_CONFIG` defaults |  > [!NOTE] > **The combat formulas are already unified.** `calculateCombatV4` is just a thin wrapper that calls `calculateCombat()` from `@pax/common`, passing `GAME_CONFIG` slider values as overrides. Phase 1 of unification is complete ΓÇö only the wrapper indirection remains as cleanup.  ---  *This document describes the AS-IS state. See `ENGINE_ARCHITECTURE_TARGET.md` for the desired state.*