# Session 2026-03-08

## Focus: Audio config integration (F-149), border engine code review

### Commits
| Hash | Description |
|------|-------------|
| `0b8cc39` | feat(F-149): integrate audio settings into GAME_CONFIG and category theme system |
| `9567b91` | fix: 3 bugs in border engine code from other agent |
| `ed0b9be` | fix: shader uniform format crash (PIXI v8 value/type) |
| `5826008` | fix: add missing roundPixelsBitGl to stroke mesh shader |
| `122d87f` | fix: default DF_BORDER_ENGINE to legacy_field |
| `32b9302` | Stabilize territory settings liveness bridge (Codex) |
| `eb1daf0` | Demote lattice centerlines and add frontier graph scaffold (Codex) |
| `395eb7e` | Document staged frontier rollout (Codex) |

### 1. Audio Config Integration (F-149)
Integrated audio settings as first-class citizens in GAME_CONFIG and the category theme system. Added 45 AUDIO_* keys to GameConfigType/defaults, registered 'audio' ThemeCategory under 'appearance' super-category, rewrote AudioManager to bridge through GAME_CONFIG (auto-persisted proxy), added legacy pax-audio-* migration, and implemented file-linked offsets.
Key files: `game.config.ts`, `categoryThemes.ts`, `audioManager.svelte.ts`, `FEATURE_STATUS.md`

### 2. Border Engine Code Review
Reviewed new rendering code added by another agent (border engine routing, morph easing, stroke mesh borders). Found and fixed 3 critical bugs:
1. **Svelte runes violation**: `$:` reactive statements in runes-mode component → converted to `$derived`
2. **GLSL shader crash**: Duplicate `#version 300 es` in strokeMeshBorders.ts fragment header (PIXI's compileHighShaderGlProgram already prepends this, causing fatal "duplicate #version" GLSL compile error)
3. **PIXI v8 API mismatch**: MeshGeometry constructor doesn't accept custom attributes as plain properties → migrated to `addAttribute()` calls
Key files: `ControlsSection-Territory.svelte`, `strokeMeshBorders.ts`

### 3. Frontier Graph Scaffold (Codex)
Codex agent implemented the canonical `frontierGraph.ts` module with:
- Analytic `solvePlayerPairRoots()` using piecewise-linear branch decomposition
- `computeLaneFrontiers()` → `buildFrontierGraphFromGraph()` → `extractPolylinesFromFrontierGraph()`
- Clean types: `FrontierNode`, `FrontierEdge`, `FrontierGraph` with `pairId`, `source`, `sourceRef`
- Legacy `centerlineGraph.ts` properly demoted (renamed export to `buildLegacyCenterlineGraphsFromOwnerGrid`)
- Settings liveness bridge via `registerCategoryPresetApplyCallback`
- `DEFAULT_GAME_CONFIG` frozen snapshot for clean reset
- `DF_BORDER_ENGINE` added to visual fingerprint
Key files: `frontierGraph.ts`, `centerlineGraph.ts`, `game.config.ts`, `categoryThemes.ts`, `panelSync.ts`

### Deferred Work
- Wire `frontierGraph.ts` into canonical border branch (Stage 2A + Stage 2B/4 merged frontier)
- Switch mesh borders off the legacy owner-grid source
- Backfill visible fills from same frontier geometry
- Remaining open bugs (B-24 through B-37, R-1)
- UI updates to expose new audio config options in settings panel
