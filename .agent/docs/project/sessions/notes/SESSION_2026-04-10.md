# Session notes — 2026-04-10

## Map lane clearance + single-source CX (P0)

- **`@pax/common/mapgen`:** `MapGenConfig.mapgenStarMarginPx` / `mapgenLaneBufferPx`; `generateConnections(..., passThroughClearancePx)` defaults to 75 (45+30); `generateMap` passes `msr + buffer`.
- **Client:** `GAME_CONFIG.MAPGEN_LANE_BUFFER_PX` (default 30); `generateMap` / preview / standard init pass MSR + buffer from `GAME_CONFIG`.
- **Live tuning:** `gameStore.rebuildConnectionsFromLaneClearance()`; Map & Grid (`ControlsSection-Visuals`) sliders for MSR + lane buffer when `hasStarted` — rebuilds Delaunay links + `bumpTerritoryVisualConfig`.
- **CX module:** `pax-fluxia/src/lib/territory/corridor/buildCorridorVirtualSites.ts` — **no same-owner guard**; cross-owner uses midpoint split + `anchorStarId`. `computeCorridorVirtuals` delegates; **Metaball**, **Modified Voronoi**, **Pixel** (split capsule) updated.
- **Server:** `GameRoom` reads optional `gameplayConfig.MODIFIED_VORONOI_STAR_MARGIN` / `MAPGEN_LANE_BUFFER_PX` for mapgen.
- **Plan doc:** `.agent/docs/project/implementation-plans/2026-04-10/MAP_LANES_MSR_BUFFER_AND_CROSS_PLAYER_CX.md`

**Not in this pass:** curved lane solver, `LanePath` for ship animations, per-family CX weights, custom-map validator.
