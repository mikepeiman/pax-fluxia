# Doc epic synthesis (final) — territory rendering

**Date:** 2026-04-08  
**For:** Architect + Impl 0 agent (read after `handoff_doc_c.md`).

## What the doc phases produced

- **~28 tracked idea rows** (I-001 … I-028) + Doc C additions in **BRAINSTORMING_IDEAS_INDEX_FINAL.md** — rendering, geometry, transition, VFX, UX, perf, process.
- **Three artifact tiers** (`artifacts_doc_a/`, `artifacts_doc_b/`, `artifacts_doc_c/`) — ledgers, claims, contradictions, scorecard hooks, **transition inventory** grounding.
- **Single entry:** `territory-rendering-jumpstart.md` Section 0.

## Strongest evidence-backed themes

1. **Distance field** and **shader-native** morph deserve first implementation slice after gated `RenderFamily` shell (unified plan II.5 + ideas I-002, scorecard).
2. **Vector / 4-layer** path remains large but **transition layer** is the historical failure cluster — family boundary contains blast radius (Parts I–II).
3. **Human + recorder** over agent status for “works” (contradiction register).
4. **Config/UI parity** (`GAME_CONFIG` ↔ settings) matters for per-family tunables (Mar 1 session + I-014 gate).

## Contradictions resolved for engineering (see `CONTRADICTION_ADJUDICATION.md`)

- **Render Family** outside; **geometry-refactor** inside **VectorPolygonFamily**; **blueprint** = secondary idea source unless architect reopens.

## What Impl 0 should **not** do

- Do not re-run full doc ingestion; extend **BRAINSTORMING_IDEAS_INDEX_FINAL.md** only if code discovers new ideas.
- Do not treat Perplexity round **duplicates** as action items without synthesis.
