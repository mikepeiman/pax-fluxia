---
date created: 2026-06-12
last updated: 2026-06-12
last updated by: AI
relevant prior docs:
  - .agent/docs/sessions/2026-06-11/2026-06-11_graphify-and-provider-cache-plan.md
superseding docs:
---

# 2026-06-12 Chat

## User

```text
Install and test Graphify now.
```

## User

```text
Unaccepted. 
You produced ambiguous conclusions and no actual comparisons for tokens and time on tasks.
What would this look like "deliberate full-code graph strategy"?
What needs to be done to properly test and use this tool?
```

## User

```text
"Current cache prefix is about 34k estimated tokens, with a stable prefix hash recorded by the benchmark path." what does this mean?
```

## User

```text
That seems like a MASSIVE prompt!!! Soujds extremely wasteful and impeding of agent effectiveness. Audit this. 
```

## Outcome

- Audited the provider prompt-cache prefix and found the previous default was too large: about 34,240 estimated tokens.
- Root cause: the provider-cache prefix embedded all stable artifact contents, including a roughly 21k-token project architecture artifact, instead of using them as on-demand context.
- Changed provider-cache generation to default to an artifact-index/routing prefix with full artifact contents omitted by default.
- New provider prefix benchmark: 1,086 estimated tokens, 4,342 chars, threshold pass, 4,096-token budget pass.
- Added `bun run agentic:context:audit`, which writes `.agent-harness/metrics/context-cache-audit-latest.md` and `.agent-harness/metrics/context-cache-audit-latest.json`.
- Current audit result: provider prefix is 3.2% of the full local artifact bundle and avoids about 32,701 estimated provider-prefix tokens by default.

---

# Session: project cleanup + architecture audit + consolidation prep (Claude Code)

## User

```text
Perform a project cleanup for dead code, redundant code, erroneous code and files, poor architecture, uncertain intent/purpose, etc. We want a full cleanup and reduce cruft.
```

## User

```text
I refreshed git auth, continue
```

## User

```text
You audit seemed weak. What are the top 10 largest files in the project? GameCanvas? How many LOC? What are best practices? This is the big work. In addition, I specified architecture be included; you should be scanning architecture docs, and mapping the project structure and systems interconnections against architectural standards.
```

## User

```text
We're on master, follow @.agent/AGENT.md protocols, commit.
You need to look for the most recent architecture guidance, WRT rendering we are using a "renderer families" approach, while the 4-stage pipeline is recognized as applying to vector methods. "Families" is the most current architecture development. 

WRT geometry, I do not know which one is best right now. That's why we have so much "legacy" code around. Many things in development but not finished. And we haven't even merged in several worktrees yet, either. That's coming soon.

I think finalizing and truly deleting the extraneous geometry generators requires dealing with rendering also; as that dictates or specifies and constrains the shape of the geometry/ownership data needed. And I've had to go deep, deep into the logic and pseudo-algorithm for geometry to transition rendering, because agents failed to achieve success through dozens or hundreds of attempts over several months. Thoughts?

So, re #4 the answer is no, not deleting any of these yet. We must first establish success, and get all working modes together (spread across worktrees currently) so we are on the same page - the same branch and repo. Then we can determine what is truly ready for the trashbin, and what we keep as prototype for future work.  Some old modes may have value for future still - or not, I don't know.

So, let's do this on a new branch and worktree now. Create a new plan outline summary given my feedback here, and we'll get ready to implement.
```

## Outcome

- Cleanup committed on master (`a3fd4ac42`): scratch dumps, stale design dirs (pencil/, website_cursor_pencil/, .colyseus-demo/), committed build artifacts (common/dist, pax-server/dist untracked + gitignored), one-off scripts, deprecated calculateRepair(). 56 files, -77,091 lines.
- Compatibility shims (flat ui/*.svelte, menuDefs, menuTheme) and ui/_archived were initially deleted, then RESTORED before commit: git grep across all 34 branches showed every unmerged branch still references those paths (MULTI_LANE_WORKTREE_GUIDE.md §2.3 documents them as intentional shims).
- Architecture audit delivered (LOC tables, god-file inventory, conformance vs documented standards). Corrected per user feedback + TERRITORY_RENDER_SYSTEM_CURRENT.md: render families are the current architecture direction; the 4-layer pipeline applies to the vector route; legacy geometry/render modes are intentionally retained pending evaluation; NO deletions until worktrees are consolidated and success is established.
- New consolidation plan outline written: .agent/docs/sessions/2026-06-12/2026-06-12_worktree-consolidation-plan.md
- New branch + worktree created for consolidation work.
