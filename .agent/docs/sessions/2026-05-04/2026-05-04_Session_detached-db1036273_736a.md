# Session - 2026-05-04

## Menu exit, mode-local persistence, and territory invariants

- Started a regression-repair pass focused on:
  - game continuing after returning to menu
  - missing confirm-leave behavior
  - missing per-mode localStorage persistence
  - border/fill divergence
  - disappearing territory regions after conquest
- Initial spec alignment:
  - `.agent/docs/game/ui/TERRITORY_SETTINGS_REFERENCE.md` treats `Phase Field`, `Phase Edges`, and `Ember Lattice` as distinct mode-local surfaces.
  - `.agent/docs/game/territory/CONQUEST_ANIMATION_SPEC.md` and `.agent/docs/game/territory/TERRITORY_ARCHITECTURE.md` still require fill and border to derive from the same frontier truth.
- First implementation findings:
  - `GameContainer.svelte` already shows a leave-game confirm modal, but `confirmExit()` only changes `gameStore.currentView` and does not unmount the route-level game shell.
  - `+page.svelte` owns actual game-shell mount/unmount via `showGame`, so menu return is currently miswired across the route/container boundary.
  - `panelSync.ts` persists one shared `pax-fluxia-panel-settings` blob, which directly contradicts the documented mode-local settings model for the three newer modes.
- Current next steps:
  - rewire leave/menu flow through an explicit shell-exit event
  - split mode-local settings persistence without changing the userâ€™s current values
  - audit geometry/render invariants for the newer territory modes before patching the missing-region regression

