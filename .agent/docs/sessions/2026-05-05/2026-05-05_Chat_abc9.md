# Chat Log - 2026-05-05

## User

These do work. There is some potential, but they'll need a lot of revision.
One issue to fix, you have not used the standard control panel wiring; the intensity values (and I assume others) are not being stored/loaded from localstorage on reloads.

## Assistant

I treated that as a persistence wiring regression, not a renderer problem.

I traced the full path:

- slider input in `ControlsSection-Visuals.svelte`
- `updateVisual(...)` in `GameSettingsPanel.svelte`
- visual persistence helpers in `panelSync.ts`
- reload hydration through `loadVisuals()` / config sync

I then moved the visual-setting normalization/update logic into `panelSync.ts`, rewired `GameSettingsPanel.svelte` to use immutable visual state replacement instead of in-place mutation, and added a localStorage round-trip test covering live background tunables plus per-player background selections.
