# 2026-05-05 Session

## Scope

- Worktree: `C:\Users\mikep\.codex\worktrees\dcc7\pax-fluxia`
- Branch: `codex/render-infra/pvv4-transition-bets`
- Focus:
  - trace the real ownership -> geometry -> topology -> transition data pipeline
  - compare PVV4 against phase/perimeter families
  - define the export artifact stack precisely
  - identify architecture and semantic failures surfaced by the dialogue

## Current Tasks

1. Preserve the architecture dialogue losslessly in session docs.
2. Create a separate decisions/definitions record for the challenged and corrected terms, invariants, and architectural judgments.
3. Mirror durable facts into tracked worktree docs:
   - daily queue
   - worktree handoff
4. Record the newly re-surfaced region-ID failure:
   - centroid-derived region identity remains present in the live vector geometry compiler
   - this is now explicitly logged as invalid

## Facts

- The live vector geometry compiler still creates region IDs from a quantized polygon centroid:
  - `region:${ownerId}:${Math.round(cx)},${Math.round(cy)}`
  - producer:
    - `C:\Users\mikep\.codex\worktrees\dcc7\pax-fluxia\pax-fluxia\src\lib\territory\layers\geometry\compiler_UnifiedVectorGeometry.ts`
- The `pvv2:` prefix seen in geometry/topology versions is stale fingerprint residue from the geometry fingerprint builder, not proof that the old PVV2 renderer is executing:
  - `C:\Users\mikep\.codex\worktrees\dcc7\pax-fluxia\pax-fluxia\src\lib\territory\compiler\powerVoronoiTerritoryGeometryGenerator.ts`
  - `C:\Users\mikep\.codex\worktrees\dcc7\pax-fluxia\pax-fluxia\src\lib\territory\layers\geometry\planners\GeometryFingerprint.ts`
- Topology IDs are currently coordinate-composite IDs:
  - vertex IDs like `1113.36,331.86`
  - section IDs like `1113.36,331.86->981.34,269.35:ai-3|ai-4`
  - producer:
    - `C:\Users\mikep\.codex\worktrees\dcc7\pax-fluxia\pax-fluxia\src\lib\territory\compiler\buildFrontierTopology.ts`
- Transition diagnostics in exported packages are built from already-normalized runtime truth, not raw `TerritoryFrameInput`:
  - recorder begins at `SnapshotCaptureContext`
  - package manifest compacts geometry/topology further

## Corrected Meanings

- `bundle`
  - in-memory recorder artifact
  - type: `TransitionDebugBundle`
- `package`
  - exported diagnostic artifact created from a bundle
  - manifest type: `DiagnosticPackageManifest`
- `anchorKey`
  - stable-anchor pair key
  - not a user-level change-anchor name
- `evaluation = animated_fronts`
  - means at least one active front was planned
  - does not mean the transition is visually correct or acceptable

## Challenged / Corrected Architectural Points

1. Shared geometry truth is still not truly unified across PVV4 and the field/perimeter families.
2. `GameCanvas` still manufactures transition truth for some families, which is an architectural violation.
3. The recorder/export stack starts after the first major normalization step, which hides the raw game graph from diagnostics.
4. The region-ID scheme is coupling identity to geometry output, which is structurally wrong for continuity and transition planning.

## Centroid-ID Failure: Logical Consequence

A centroid cannot serve as region identity because identity must survive exactly the kinds of local boundary changes that move the centroid.

Why the current scheme guarantees failure:

1. Region identity is supposed to answer continuity:
   - is this the same owned region across frames?
2. A centroid is a function of the whole polygon:
   - any capture, split, merge, or border motion changes it
3. Therefore the ID changes when continuity is most important
4. That produces churn in:
   - region IDs
   - loop/region matching
   - transition continuity
   - diagnostics readability
5. Once identity is unstable, downstream code compensates heuristically and starts collapsing, respawning, or mispairing the wrong structures

## Better Direction

Region identity should come from gameplay/connectivity continuity, not geometric centroid coincidence.

The likely correct identity substrate is:

- owner ID
- connected star-component membership
- continuity matching against previous frame using star membership overlap and structural adjacency

## Memory / Context Management Note

- Atlas chatlog exists but was not auto-capturing this dialogue.
- The session docs created today are therefore the primary durable record for this architecture-analysis exchange.
- The worktree handoff and daily queue are also updated so the branch-level durable record stays aligned.
