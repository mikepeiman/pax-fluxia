# Session - 2026-05-01

## Focus
- Fix the map/viewport relationship in the preferred Phase Edges presentation so the map is centered and territory fills reach the viewport/grid-area boundaries.

## Facts
- The user observed a real asymmetry: fills reached the top and left bounds, but not the bottom and right.
- `GameCanvas.svelte` was fitting the world to a star-derived bounding box while territory renderers were also relying on `GAME_WIDTH` / `GAME_HEIGHT` as their world size.
- That is a bad architectural pattern: star extents are not the authoritative map rectangle.
- The first attempted fix on 2026-05-01 was rejected by live user verification:
  - it introduced margins on all four edges
  - the map still read as off-center
  - the user explicitly clarified that the star map / game map is not the same contract as the territory fill bounds
- The second-order architectural defect was that shifting the territory container without localizing the renderer inputs left the renderer in a mismatched coordinate space.
- The territory presentation request signature was also missing the presentation frame key, which meant a paused scene could reuse a stale fill render after the frame changed.

## Work Done
- Read `.agent/AGENT.md` again and confirmed the daily-doc/commit protocol before editing.
- Traced the active camera-fit path in `GameCanvas.svelte`:
  - `centerAndFit()`
  - `handleResize()`
  - `applyZoomTransform()`
  - `updateWorldBounds()`
- Confirmed that `updateWorldBounds()` was deriving the world from `min/max star positions + pad`, which is not the same contract as the generated map width/height.
- Added `pax-fluxia/src/lib/components/game/worldRect.ts` with `resolveViewportWorldRect()`:
  - prefers configured map extents when present
  - expands stale configured extents if live stars exceed them
  - falls back to star extents only when no configured map rectangle exists
  - keeps the world rooted at `(0,0)` so camera fit and territory rendering share one frame
- Updated `GameCanvas.svelte` to:
  - resolve the configured map rect for the current display orientation
  - center/fit against that rect
  - feed the same rect into `GAME_WIDTH` / `GAME_HEIGHT`
- Updated `gameStore.svelte.ts` so debug maps and saved maps also seed `_MAP_WIDTH`, `_MAP_HEIGHT`, `_MAP_PADDING_X`, `_MAP_PADDING_Y`.
- Added `worldRect.test.ts` for the resolver contract.
- Re-audited the rejected first fix and split the problem into three distinct rectangles:
  - star-fit camera rect
  - stable authored/display map rect
  - viewport-aligned territory presentation frame
- Added `pax-fluxia/src/lib/components/game/territoryPresentationSpace.ts`:
  - localizes display-space stars into the viewport-aligned territory frame
  - localizes canonical geometry into the same frame, including region/frontier/shell/topology coordinates
  - caches localized geometry by raw geometry reference + frame key
- Rewired `GameCanvas.svelte` so territory presentation now:
  - includes the territory frame key in the paused/live presentation signature
  - uses localized stars for direct territory renderers
  - uses localized stars and localized geometry for render-family inputs
  - uses localized stars/world size for canonical bridge and canonical controller presentation paths
- Added `territoryPresentationSpace.test.ts`.
- Wrote a dedicated post-mortem for the failed first viewport fix because the earlier implementation was architecturally wrong and user-corrected.

## Validation
- `bun x vitest run src/lib/components/game/worldRect.test.ts`
- `bun x vite build`
- `bun x vitest run src/lib/components/game/worldRect.test.ts src/lib/components/game/territoryPresentationSpace.test.ts`
- `bun x vitest run src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
- `bun x vite build`

## Open
- Live user verification is still required for the visual result in the running app.
- If the user still sees asymmetry after this fix, the next suspects are:
  - a territory family path that still ignores the localized presentation frame
  - a container/layout offset outside the Pixi world transform path
  - a saved-map/debug-map metadata path that still yields the wrong authored/display map rect

## Addendum - map-fit rethink after live rejection
- The later live review on 2026-05-01 rejected the viewport-aligned territory-frame theory too:
  - the map became closer to centered but still not perfectly centered
  - fills showed margin on all sides
  - the user explicitly asked for the map/fill domain to be centered and owned by the map-area contract, not by the star-fit camera rect
- Revised diagnosis:
  - the earlier three-rect model was still overfitting to the star cloud
  - the territory fill domain should not be enlarged to a viewport-sized world-space frame when the authored map rect is the thing that is supposed to fill and center
  - the correct ownership for this preferred Phase Edges mode is:
    - star-fit rect remains diagnostic/gameplay context only
    - authoritative map rect owns stage fit + centering
    - territory presentation frame must lock back to that same map rect
- Additional runtime work completed:
  - added `resolveMapFitWorldRect()` in `pax-fluxia/src/lib/components/game/worldRect.ts`
  - rewired `GameCanvas.svelte` so:
    - `handleResize()` computes `baseScale` from the map rect
    - `applyZoomTransform()`, `clampPan()`, `handleWheel()`, and `navigateToStar()` all use the map rect center for baseline placement
    - `updateTerritoryViewportFrame()` now uses the authoritative map rect instead of a viewport-sized local presentation frame
  - kept `territoryPresentationSpace.ts` in place, but for this path it now effectively becomes an identity/localization helper rather than the owner of visible map fill bounds
- Outer-border audit/fix completed in the same step:
  - identified that the centered-blended Phase Edges edge pass only built owner-owner edges from right/bottom interior adjacency
  - added first-class config `TERRITORY_FRONTIER_OUTER_BORDER_ENABLED`
  - surfaced it in `Territory Styles > Border` as `Outer perimeter border`
  - implemented a dedicated owner-vs-world perimeter pass so the outer border is:
    - optional
    - full-map
    - not a right-side leak
- Validation for this addendum:
  - `bun x vitest run src/lib/components/game/worldRect.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun x vite build`
- Remaining acceptance:
  - live user verification that the map now centers perfectly
  - live user verification that fills now reach the intended map-area bounds
  - live user verification that the new outer perimeter toggle is symmetric and optional

## Addendum - correction after the map-rect centering step reversed the improvement
- The user then clarified the requirement more precisely:
  - the star-map bounds are what must be centered
  - the territory fill domain must extend equally around that centered star-map view
  - the previous map-rect centering step had reversed the earlier improvement
- This means the map-rect centering theory was a misread of the requirement, not just an imperfect implementation.
- Runtime correction:
  - restored the stage baseline in `GameCanvas.svelte` to use the star-fit/content rect again for:
    - resize fit
    - zoom transform baseline
    - pan clamping baseline
    - wheel-anchor baseline
    - `navigateToStar()` baseline
  - kept the explicit outer-perimeter toggle/path from the previous step
  - kept the viewport-aligned territory presentation frame centered on the star-fit rect
- Validation for this correction:
  - `bun x vitest run src/lib/components/game/worldRect.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun x vite build`
- New acceptance target:
  - star-fit centered again
  - fill frame extending symmetrically around that centered star-fit view
  - optional outer perimeter remaining consistent

## Addendum - localized grid phase and clipped-frame perimeter correction
- The next live user report narrowed the defect:
  - the result was close
  - there was still a right-side-only fill margin
  - optional outer-edge borders were still only showing on the right
- Important diagnosis shift:
  - this was no longer a generic centering-baseline problem
  - the localized territory presentation frame was being treated as a fresh `0`-anchored grid by the Phase Edges classifiers
  - that loses the underlying world-grid phase whenever the star-fit-centered presentation frame starts at a non-zero `minX/minY`
  - separately, the optional outer perimeter border was still being inferred from sampled grid-cell edges instead of the actual clipped presentation-frame boundary
- Runtime work completed:
  - extended the render-family world contract to carry `minX/minY` in:
    - `pax-fluxia/src/lib/territory/families/RenderFamilyTypes.ts`
    - `pax-fluxia/src/lib/territory/families/buildRenderFamilyInput.ts`
  - rewired `GameCanvas.svelte` so localized territory render-family inputs now pass:
    - `worldMinX: territoryPresentationFrame.minX`
    - `worldMinY: territoryPresentationFrame.minY`
  - updated `buildGridClassification.ts` so localized presentation frames preserve the global grid phase instead of restarting a fresh local phase at `0`
  - updated both grid families and the metaball-grid plan worker request types so the preserved phase is used consistently in:
    - cached plan keys
    - direct classification builds
    - worker plan requests
  - replaced the old owner-vs-world perimeter leakage path in `MetaballGridPhaseEdgesFamily.ts` with a clipped-frame outer-perimeter pass:
    - derives border segments from the actual presentation-frame boundary
    - merges them by owner and side
    - draws them inset by half the stroke width so the frame-edge border is visible on all sides instead of clipping away on three sides
- Validation:
  - `bun ./node_modules/vitest/vitest.mjs run src/lib/territory/families/metaballGrid/buildGridClassification.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun ./node_modules/vitest/vitest.mjs run src/lib/territory/families/metaballGrid/buildGridClassification.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts tools/debug/benchmark-frontier-techniques.test.ts`
  - `bun x vite build`
- Queue added for the next pass:
  - after live verification of the right fill margin and all-sides outer perimeter, inspect the end-of-transition jank/disjointness the user observed

## Addendum - border-layer ownership correction after the localized-phase fix did not change the visible result
- The user reported that the prior localized-phase/perimeter change had not visibly solved the new acceptance issues:
  - the right-side-only margin still appeared
  - outer map borders now appeared on three sides except the right
  - `Border Mode = off` still left one surviving border draw under the fills
  - `Border Mode = territory_edge` produced two essentially identical borders, slightly offset
- Important diagnosis:
  - this was not one generic border bug
  - there were two separate render-path ownership failures
    - clipped-frame outer perimeter still missed the exact-right-edge contact case
    - Phase Edges was allowing non-selected border layers to survive under the shared-edge control path
  - there was also a structural gating bug:
    - centered-blended shared-edge borders were not restricted to the `shared_edge` border source
    - phase-derived contour/shader borders were not restricted by `Border Mode`
- Runtime corrections completed in `pax-fluxia/src/lib/territory/families/metaballGrid/MetaballGridPhaseEdgesFamily.ts`:
  - clear/hide dormant frontier border layers every frame:
    - `frontierGraphics`
    - `frontierMeshLayer`
    - unused frontier shader layers
  - gate phase-derived border presentation behind:
    - `borderMode !== 'off'`
    - positive effective border width
    - positive effective border alpha
    - the selected surface recipe actually using phase borders
  - restrict centered-blended shared-edge borders so they only run when:
    - border mode is active
    - blending is enabled
    - distribution is square
    - the selected frontier border source is `shared_edge`
  - loosen clipped-frame perimeter boundary tests to treat exact frame-edge contact as inclusive, so the right side is not dropped when the outer cell lands exactly on the frame boundary
- Regression coverage added in `pax-fluxia/src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`:
  - `Border Mode = off` clears stale/dormant border layers
  - shared-edge `territory_edge` keeps visible borders on the base border layer only
- Validation for this addendum:
  - `bun ./node_modules/vitest/vitest.mjs run src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts src/lib/territory/families/metaballGrid/buildGridClassification.test.ts`
  - `bun x vite build`
- Remaining live acceptance after this code step:
  - verify the missing right-side perimeter is now present
  - verify `Border Mode = off` leaves no visible border path
  - verify shared-edge `territory_edge` no longer duplicates
  - if the right-side fill margin still remains after those are corrected, continue the fill-support audit before touching the queued transition-end jank

## Addendum - Phase Edges render-contract audit before more tuning work
- The user requested a full audit of literally every value affecting the current preferred `metaball_grid_phase_edges` render mode, plus a control audit for which of those values are actually surfaced in UI and correctly wired.
- Important architectural framing:
  - the honest contract is larger than `MetaballGridPhaseEdgesFamily.ts`
  - this mode consumes both direct family tunables and inherited geometry/topology tunables from `buildPerimeterFieldRenderFamilyGeometry()` / `readTerritoryGeometryTunables()`
  - failing to include that underlayer would hide real render-affecting inputs from the audit
- Audit findings recorded:
  - direct family tunables were confirmed from `METABALL_GRID_TUNABLE_KEYS` plus `TERRITORY_FRONTIER_TUNABLE_KEYS`
  - inherited geometry/topology tunables were confirmed from:
    - `pax-fluxia/src/lib/territory/families/buildFamilyGeometry.ts`
    - `pax-fluxia/src/lib/territory/geometry/geometryTuning.ts`
  - hidden/non-UI affecting values for the current Phase Edges mode were confirmed:
    - `VORONOI_BORDER_SMOOTH`
    - `CHAIKIN_BOUNDARY_PAD`
    - `CHAIKIN_BOUNDARY_EPS`
    - `TERRITORY_CLUSTER_SPLIT`
  - semantically misleading but wired UI value confirmed:
    - `FRONTIER_RESOLUTION` affects the Phase Edges geometry underlayer, but it is surfaced under `Territory Tuning & Constraints > Border Transition`
  - UI duplication confirmed for the current mode:
    - `METABALL_GRID_CELL_SHAPE`
    - `METABALL_GRID_CELL_INSET_PX`
    - `METABALL_GRID_CELL_CORNER_PX`
    - `METABALL_GRID_BORDER_MODE`
    - `METABALL_GRID_BORDER_BLEND`
    - `METABALL_GRID_BORDER_CHAIKIN_PASSES`
    - `METABALL_GRID_EDGE_SMOOTHING_PASSES`
    - `METABALL_GRID_EDGE_TRIM_PX`
  - gating mismatch confirmed:
    - the `MetaballGridTuning.svelte` copies of `Shared Edge Smoothing` and `Shared Edge Trim` have weaker applicability gating than the `TerritorySurfaceStyleTuning.svelte` copies, so they can still be adjusted in some states where they will not visibly apply
- No runtime code was changed in this step; this was a structure/ownership audit to prepare the next fix pass and to avoid losing hidden-contract knowledge on merge-back.

## Addendum - outer perimeter contract bug and junction-control gating clarification
- After the contract audit, the user supplied a precise live report:
  - `Outer perimeter border` still had no visible result, on or off
  - the previously broken shared blended borders were now fixed
  - `Junction Render` was shown as inactive and had effectively never been reachable in the user's preferred rounded Phase Edges configuration
- Important diagnosis:
  - `Outer perimeter border` was not a settings propagation bug
  - the toggle state was reaching the family, but its draw path lived inside the centered-blended shared-edge branch
  - that meant the perimeter pass was structurally owned by the wrong border family and became inert whenever the preferred rounded path was active
  - `Junction Render` was disabled for a coherent reason, but the UI did not surface that reason clearly enough
- Runtime correction in `pax-fluxia/src/lib/territory/families/metaballGrid/MetaballGridPhaseEdgesFamily.ts`:
  - promoted `trueHalf` to the outer paint scope
  - introduced a dedicated `shouldDrawOuterPerimeter` gate
  - expanded `needsEffectiveColorIdxByGridIdx` so the owner-perimeter pass can build its occupancy truth even when the centered-blended shared-edge branch is not active
  - moved `drawOuterPerimeterIntervals(...)` out of the centered-blended shared-edge branch and into its own independent perimeter pass after base border drawing
- UI clarification in `pax-fluxia/src/lib/components/ui/settings/TerritorySurfaceStyleTuning.svelte`:
  - added `sharedEdgeControlGateReason()`
  - disabled `Shared Edge Smoothing`, `Junction Render`, `Junction Gap Trim`, and `Junction Bubble Radius` now surface the exact unmet requirement instead of just appearing inert
  - the dominant reason in the user's preferred look is: `Frontier Border Geometry = Straight shared edge` is required
- Validation:
  - `bun ./node_modules/vitest/vitest.mjs run src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun x vite build`
## Human Input 10
```text
1. Borders do appear around all four sides of map now
2. A weird glitch when I used `f11` fullscreen mode in Chrome: I lose top-bottom borders! (2 images attached)
3. Fill does not match border at map edges (edge border does not even match junction border) 
4. Fill is still offset away from borders within territory. I need the slider adjustment for offset, and the toggle to turn it off, so fills by default fill completely.
5. Yes, I said I wanted it, so I want it. I don't like your tone when I ask for things. You should take the natural approach of at least wondering and asking "Do you want this?" And you would default to assume I do want what I ask for, rather than an explanation of why it is unavailable to me (arbitrary, I directed the development of this entire thing!)
```

## Machine Notes 10
- Accepted the user's live report as the current spec for this pass:
  - normal window now has four-sided outer borders
  - Chrome `F11` fullscreen drops top/bottom borders
  - fill still does not align with border at the map edge
  - fill still pulls back from borders even with `Inward Offset = 0`
- Found the hidden pullback path:
  - boundary fill was still inheriting `Cell Inset` and `Junction Gap Trim`
  - that made `Inward Offset` non-authoritative and caused zero-offset fill pullback
- Implemented a shared boundary-fill ownership correction:
  - added `computeBoundaryInset(...)` in `pax-fluxia/src/lib/territory/families/metaballGrid/edgeShaping.ts`
  - added `METABALL_GRID_BOUNDARY_FILL_FLUSH` to the config/tunable contract
  - surfaced `Flush Boundary Fill` in `pax-fluxia/src/lib/components/ui/settings/TerritorySurfaceStyleTuning.svelte`
  - set Phase Edges default boundary-fill behavior to flush
  - updated both `MetaballGridFamily.ts` and `MetaballGridPhaseEdgesFamily.ts` to use the shared helper
- Implemented a fullscreen-safe outer-perimeter correction:
  - outer perimeter is no longer keyed to fullscreen/local presentation-frame dimensions
  - Phase Edges now derives perimeter bounds from occupied territory cells
- Added regression coverage in `pax-fluxia/src/lib/territory/families/metaballGrid/edgeShaping.test.ts`
- Validation completed with:
  - `bun x vitest run src/lib/territory/families/metaballGrid/edgeShaping.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun x vite build`
- Process/tone correction recorded:
  - when the user explicitly asks for a feature, default posture should be implementation ownership rather than explaining why the current branch does not expose it

## Human Input 11
```text
There is still that inset gap; your tuning slider `inward offset` still does nothing; and the fix will be found simply by comparison of `centered-blended borders` on|off; when toggled off, the fills are correct. When toggled on, they are inset.
```

## Machine Notes 11
- Treated the user's comparison as a precise branch isolation, not a symptom note:
  - `Centered-blended borders = Off` -> fill path is acceptable
  - `Centered-blended borders = On` -> fill path becomes inset
- Root cause found:
  - the shared-edge recipe in `pax-fluxia/src/lib/territory/frontier/surface.ts` still advertised `usesPhaseFill = true`
  - that let the centered-blended shared-edge branch activate the phase fill-replacement path even though the user was only toggling a border style
  - this was a recipe/consumer ownership bug: a border toggle was still suppressing/replacing fill geometry
- Corrections implemented:
  - changed the shared-edge recipe to:
    - `fillSource = scene_cells`
    - `usesPhaseFill = false`
  - updated `MetaballGridPhaseEdgesFamily.ts` so the shared-edge centered-blended path no longer builds/renders phase fill replacement
  - added `METABALL_GRID_BOUNDARY_FILL_FLUSH` into the paint signatures of both `MetaballGridFamily.ts` and `MetaballGridPhaseEdgesFamily.ts` so dirty-frame reuse cannot hide that toggle
- Regression coverage expanded in `pax-fluxia/src/lib/territory/frontier/frontier.test.ts`:
  - shared-edge recipe now asserts scene-cell fill ownership
  - contour/shader recipes still assert phase-surface fill ownership
- Validation completed with:
  - `bun x vitest run src/lib/territory/frontier/frontier.test.ts src/lib/territory/families/metaballGrid/edgeShaping.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun x vite build`

## Human Input 12
```text
False. Your fix completely fails and nothing has changed.
Reminder, also `Flush Boundary Fill` does nothing. 
The fill works when `centered-blended` is off. Seriously, just fully code diff the two modes!
```

## Machine Notes 12
- Took the user's rejection as authoritative and re-audited the actual visual delta between the two branches instead of defending the previous recipe fix.
- New diagnosis:
  - the previous recipe correction was valid but insufficient
  - the remaining mismatch was geometric:
    - square-cell fill still used a uniform four-sided inset
    - centered blended borders draw on the true shared edge
    - with nonzero `Cell Inset`, that guarantees a visible gap even when no phase fill overlay is active
  - this also explains why `Flush Boundary Fill` and `Inward Offset` appeared inert: they only touched the boundary inset, while the visible gap was still being dominated by uniform per-cell shrink
- Implemented directional square-cell boundary fill:
  - added `computeSquareCellEdgeInsets(...)` in `pax-fluxia/src/lib/territory/families/metaballGrid/edgeShaping.ts`
  - same-owner sides keep `Cell Inset`
  - frontier-facing sides and outer-perimeter sides now use boundary inset ownership
  - updated both `MetaballGridPhaseEdgesFamily.ts` and `MetaballGridFamily.ts` to draw square fills from per-edge insets instead of one uniform inset when the true-edge border path is active
- Regression coverage added in `edgeShaping.test.ts`
- Validation completed with:
  - `bun x vitest run src/lib/territory/frontier/frontier.test.ts src/lib/territory/families/metaballGrid/edgeShaping.test.ts src/lib/territory/families/metaballGrid/MetaballGridFamily.test.ts`
  - `bun x vite build`
