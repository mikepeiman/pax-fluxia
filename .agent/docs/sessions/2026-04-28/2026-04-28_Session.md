# Session - 2026-04-28

## Purpose
Resume the gameplay-performance lane from the 2026-04-27 handoff and continue autonomously until the next stable validation and commit point.

## Starting State
- Governing plan:
  - `pax-fluxia/docs/gameplay-performance-findings-and-plan-2026-04-27.md`
- Handoff:
  - `.agent/docs/sessions/2026-04-27/2026-04-27_gameplay-performance-handoff.md`
- Immediate priority:
  - rerun the 20-minute `distanceFieldGameplay` soak on the newest code
  - inspect the resulting diagnostics before changing code

## Notes
- Keep commits regular.
- Keep the gameplay-performance scope isolated from unrelated dirty work already present in the repo.
- `bun run build` at repo root still exits nonzero because the workspace `pax-server` build imports the Bun builtin `bun` in a browser-targeted bundle path. The client-only validation command remains `bun run build` in `pax-fluxia/`.
- Ran `bun run debug:browser-gameplay-summary` to refresh the canonical pointer before taking a new soak.
- Ran a fresh 20-minute `distanceFieldGameplay` soak on the newest pre-patch code:
  - artifact: `.agent-harness/metrics/browser-gameplay-benchmark-2026-04-28T12-29-39-403Z.json`
  - result: `17.516ms avg`, `16.8ms p95`, `66.8ms max`, `3388` frames over `33ms`
  - long-task and long-animation-frame counts stayed at zero
  - overlay churn stayed fixed at `0.029ms avg`, but late-game average still missed target
- Investigated the active ship path in `pax-fluxia/src/lib/renderers/ShipRenderer.ts` and confirmed the optimized runtime already hoisted some per-star work, but it still recomputed slot-normalization and phase-sine math per ship.
- Landed a ship-path follow-up in:
  - `pax-fluxia/src/lib/utils/render.utils.ts`
  - `pax-fluxia/src/lib/renderers/ShipRenderer.ts`
- Change summary:
  - extended `getOrbitSlot(...)` to return cached `angle`, `radius`, `ndx`, and `ndy`
  - reused those cached slot values in the active orbital ship loop
  - added a fixed `SHIP_PHASE_AMPLITUDES` lookup table to avoid per-ship trig work in the steady-state orbit path
- Client validation after the patch:
  - `bun run build` in `pax-fluxia/` passed
- Reran the 20-minute `distanceFieldGameplay` soak after the patch:
  - artifact: `.agent-harness/metrics/browser-gameplay-benchmark-2026-04-28T12-56-54-746Z.json`
  - screenshot dir: `.agent-harness/metrics/browser-screenshots/2026-04-28T12-36-45-498Z`
  - result: `17.127ms avg`, `16.8ms p95`, `83.4ms max`, `1862` frames over `33ms`
- Delta vs the pre-patch 20-minute soak:
  - average frame improved by `0.390ms`
  - `over33MsCount` improved by `1526`
  - `game.renderFrame.ships` improved by `0.014ms avg`
  - `game.renderFrame.shipParticleUpdate` improved by `0.033ms avg`
  - `game.renderFrame.stars` improved by `0.130ms avg`
  - `game.renderFrame.stars.labels` improved by `0.079ms avg`
- Remaining issue after this pass:
  - the soak still misses the `avgFrameMs < 16.7` target
  - the remaining worst misses are still predominantly fully unattributed browser stalls rather than measured render work
- Evening smoothness-first correction pass:
  - confirmed `common/resources/settings-live/current-settings.json` is output only and not a runtime input source
  - traced actual startup tunables to persisted panel settings plus `GAME_CONFIG`
  - added a persisted-settings migration for legacy coarse `metaball_grid` defaults:
    - spacing `48 -> 32`
    - flip transition `hard -> dual_pass_blend`
    - flip window `0.06 -> 0.14`
    - timing scatter `0.02 -> 0`
  - updated the family defaults in `pax-fluxia/src/lib/territory/families/metaballGrid/config.ts` to the same smoothness-first values
  - kept the retained-sprite square-cell fast path in `pax-fluxia/src/lib/territory/families/metaballGrid/MetaballGridFamily.ts`
  - renamed the active ship-cap path from stale `shipLod` semantics to `shipVisualCapPlan`
  - tightened the territory settings copy around the fill path so the topology route and legacy fill-only fallbacks are described accurately
- Validation for the evening correction pass:
  - targeted tests passed:
    - `pax-fluxia/src/lib/territory/integration/TerritorySettingsBridge.test.ts`
    - `pax-fluxia/src/lib/territory/buildTerritoryConfigFingerprint.test.ts`
    - `pax-fluxia/src/lib/lanes/lanePolylineCache.test.ts`
    - `pax-fluxia/src/lib/lanes/applyLaneTravelPath.test.ts`
    - `pax-fluxia/src/lib/lanes/laneConnectionSync.test.ts`
  - `bun run build` in `pax-fluxia/` passed
  - focused artifact:
    - `.agent-harness/metrics/browser-gameplay-benchmark-2026-04-28T21-12-40-609Z.json`
  - focused scenario results:
    - `metaball_gridGameplay`: `16.773ms avg`, `16.7ms p95`, one `33.3ms` spike
    - `metaball_gridOrders`: `16.665ms avg`, `16.8ms p95`, no `>33ms` spikes
    - `metaball_gridConquestDiagnostic`: correctness path passed, but timing is dominated by diagnostic capture cost
- Critical distinction from the focused conquest diagnostic:
  - `game.renderFrame.territory.transitionDiagnosticSync` averaged `11.325ms`
  - CPU hotspots were `readPixels`, `putImageData`, `drawImage`, and `generateCanvas`
  - the transition bundle path validated cleanly:
    - `schema=pv-transition-diagnostics-v1`
    - `steps=19`
    - `failing=none`
    - `finalCompare withinTolerance=true`
- Added a separate conquest shipping-path benchmark scenario in `tools/debug/benchmark-browser-gameplay.ts`:
  - `metaball_gridConquestAnimation`
  - it reuses the conquest fixture and sample order path but keeps the recorder disabled so timing reflects shipping animation instead of bundle capture/export
- Validation for the conquest split:
  - `bun run build` in `pax-fluxia/` passed
  - artifact:
    - `.agent-harness/metrics/browser-gameplay-benchmark-2026-04-28T21-32-09-183Z.json`
  - results:
    - `metaball_gridConquestAnimation`: `16.667ms avg`, `16.7ms p95`, `16.8ms max`, `0` sampled frames over `20ms`
    - `metaball_gridConquestDiagnostic`: still correctness-valid, but timing remains dominated by capture/export cost
  - diagnostic-heavy evidence from the same artifact:
    - `game.renderFrame.territory.metaball_grid`: `7.248ms avg`
    - `game.renderFrame.territory.present.metaball_grid`: `7.244ms avg`
    - `game.renderFrame.territory.transitionDiagnosticSync`: `6.86ms avg`
    - CPU hotspots: `getPixels`, `putImageData`, `drawImage`, `generateCanvas`
- Updated interpretation:
  - shipping conquest smoothness and conquest diagnostic correctness now have separate benchmark surfaces
  - next code choice should come from either the remaining `metaball_gridGameplay` spike or the live lane-geometry bug, not from the diagnostic scenario timings
- Traced the live `BACKWARD ship-geometry assignment` path end to end:
  - cache read: `lanePolylineCache.ts`
  - travel geometry assignment: `applyLaneTravelPath.ts`
  - transfer-time diagnostics: `transferHandler.ts`
  - multiplayer runtime seeding path: `laneConnectionSync.ts` via `multiplayerStore.svelte.ts`
- Validated the active lane-geometry travel-path changes already present in the worktree:
  - short-link fallback travel no longer blends start/end off the actual lane
  - overlapping rim crossings collapse to a shared seam point instead of inverting the travel segment
  - diagnostics now treat only reversed lane ends / polylines as true backward geometry
- Completed the missing live sync fix:
  - `laneConnectionSync.ts` now accepts live star endpoints and uses them to correct reversed canonical waypoint payloads before seeding the runtime cache
  - `multiplayerStore.svelte.ts` now passes the current star positions into that seeding path
  - `laneConnectionSync.test.ts` now covers the reversed-canonical live payload case directly
- Validation for the lane-geometry pass:
  - `bun test pax-fluxia/src/lib/lanes/lanePolylineCache.test.ts pax-fluxia/src/lib/lanes/laneConnectionSync.test.ts pax-fluxia/src/lib/lanes/applyLaneTravelPath.test.ts`
  - `bun run build` in `pax-fluxia/`
