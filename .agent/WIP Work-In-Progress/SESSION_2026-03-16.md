# Session 2026-03-16

## Focus: Wiring UI to Pipeline — Style Dropdown Controls Rendering

### Commits
| Hash | Description |
|------|-------------|
| `fa9fd04` | feat: Style dropdown now controls rendering — Geometry Source dropdown added, engine bypass removed |
| `ea9b60b` | fix: remove double rendering, add None options to all dropdowns, rename Data Engine to Geometry |
| `8ce2216` | ui: remove dead controls (Distance Metric, Fill Mode), rename Morph Speed to Conquest Animation Timing, add PVV2/PVV3 labels |
| `08c2c3b` | feat: add Territory Engine (DY4) as selectable Style option in dropdown |
| `7a9b476` | fix: add missing settingsDefs mappings for fill/border transitions, add render mode diagnostic log |
| `b4cba5e` | fix: None (Off) no longer falls through backward compat to territory_engine |

### 1. Removed TERRITORY_ENGINE_ENABLED Exclusive Branch
The `if (TERRITORY_ENGINE_ENABLED)` branch in `GameCanvas.svelte` bypassed the Style dropdown and ran `renderTerritoryEngine()` exclusively. Removed this so the Style dropdown always controls rendering.
Key files: `GameCanvas.svelte` L1100-1120

### 2. Fixed Double Rendering
`renderTerritoryEngine()` drew FG2's native angular polygons, then the Style dropdown renderer drew on top — two overlaid systems. Removed the engine render call entirely. FG2 now only provides geometry data via `runFG2DataPipeline()`.
Key files: `GameCanvas.svelte`

### 3. UI Cleanup
- Added "Geometry" dropdown (FG2 Seed Graph, locked) as 4th pillar
- Added "None (Off)" to Style, Fill Transition, Border Transition
- Added PVV2/PVV3 labels to Style options
- Renamed "Data Engine" → "Geometry"
- Renamed "Morph Speed" → "Conquest Animation Timing"
- Removed dead controls: Distance Metric (Length/Hops), Fill Mode (Crossfade/Frontier)
- Kept Morph Control Points per user request
Key files: `ControlsSection-Territory.svelte`

### 4. Added Territory Engine (DY4) as Style Option
User needed the old Territory Engine render mode (DY4 animated borders) accessible from the Style dropdown. Added `territory_engine` case that calls `renderTerritoryEngine()`.
Key files: `ControlsSection-Territory.svelte`, `GameCanvas.svelte`

### 5. Fixed Style Dispatch Bugs
- Added missing `settingsDefs` mappings for `territoryFillTransition` and `territoryBorderTransition`
- Fixed "None (Off)" falling through backward compat to `territory_engine` (was checking `=== "none"`, now checks `!activeMode` for undefined only)
- Added diagnostic log `[Territory Style Dispatch]` showing `TERRITORY_RENDER_MODE → activeMode`

### Verified
All 10 style modes route correctly through the dropdown:
- territory_engine, vs_pvv3, power_voronoi, distance_field, metaball, pixel, voronoi, graph, contour, none

### Deferred Work
- Territory Engine (DY4) render mode visually broken — needs fixing after architecture refactor
- Architecture refactor: extract geometry engine from territory engine, modularize renderers
- FG2 render stage is dead code in current routing — should be cleaned up
