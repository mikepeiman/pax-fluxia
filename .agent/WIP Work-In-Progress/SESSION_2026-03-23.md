# Session 2026-03-23

## Focus: Deep diagnosis of transition pipeline failure + pipeline assumptions audit

### Commits
| Hash | Description |
|------|-------------|
| `4583454` | feat: GeometryTopologyDiff — static/drifted/spawned/vanished classification for transitions |
| `7c72cae` | revert: snap-to-target transition modes — broken interpolation reverted |
| `c23ef3a` | fix: include world border polylines in clean pipeline + add diagnostic logging |

### 1. Transition Diagnosis
Applied Wrong-Problem Detector recipe. Root cause: I was solving wrong problem. The data model (flat `[number,number][]` arrays) lacks structural provenance needed for local patch transitions. Fills and borders were interpolated independently → guaranteed divergence. Write-up in `transition_diagnosis.md`.

### 2. Revert Broken Interpolation
Reverted FrontierMorphFillMode and OptimalTransportBorderMode back to clean snap-to-target. User confirmed static rendering works correctly after revert.

### 3. Exhaustive Pipeline Assumptions Audit
User requested zero-guesswork assumptions audit. Traced entire pipeline:
- **Ownership**: `StarOwnershipSnapshotMode` (single mode)
- **Geometry**: `BoundaryAwareFrontierGeometryMode` → `computeGeometry0319` → `FrontierTopologyBuilder`
- **Transition**: `TransitionLayerCoordinator` triggers on `conquestEvents.length > 0 && geometry.version changed`
- **Presentation**: `CanonicalTerritoryStyle` → `PixiFillPresenter` / `PixiBorderPresenter`

Resolved all 14 assumptions through direct code reading. Key findings:
- A1: No dual rendering — `break` at L1399
- A6: Multiple fill regions per owner ARE possible (disconnected territories)
- A9: Fills are built from SAME smoothed polyline vertices as borders at compile time
- A10: **World border polylines were MISSING** from clean pipeline output — FIXED

### 4. World Border Fix
`FrontierTopologyBuilder.buildFrontierPolylineShapes()` only extracted `sharedPolylines` (inter-owner borders). Added `worldBorderPolylines` (owner↔world edges). This means the clean pipeline was missing all map-edge border segments.

### 5. Diagnostic Logging
Added throttled diagnostic logging to `TerritoryRuntimeCoordinator`:
- CONQUEST events (always logged)
- TRANSITION START/COMPLETE lifecycle (always logged)
- Per-second general stats: region/frontier counts, cache hits, fill/border command counts, active modes

### Deferred Work
- Implement ring-based boundary snapshot architecture for real transitions
- Derive transition fills from interpolated border geometry (single source of truth)
- Address rigid segment movement (rope/catenary interpolation instead of linear lerp)
