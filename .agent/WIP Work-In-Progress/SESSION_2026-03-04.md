# Session Notes — 2026-03-04

## Kickstart Context

**Primary task**: Implement F-144 V2 — GPU Distance Field Territory Renderer

### Background
V1 (CPU rasterization) was shelved on 2026-03-03 due to 0fps performance. V2 follows the [Deep Technical Guidance](../F-138-ModifiedVoronoi/Deep%20technical%20guidance.md) GPU pipeline: CPU Dijkstra → GPU fragment shader (PIXI.Filter) → GPU temporal blend.

### Key Files
- `DistanceFieldTerritoryRenderer.ts` — renderer module (keep Dijkstra, replace rasterize with GPU)
- `game.config.ts` — DF_ config keys (remove DF_RESOLUTION, DF_ROUNDING)
- `ControlsSection-Territory.svelte` — UI controls for DF settings
- `GameCanvas.svelte` — calls renderer at line 1165
- `RenderContext.ts` — ColorUtils interface

### Implementation Plan (4 Phases)
1. **Phase 1:** Strip CPU rasterization, keep Dijkstra + cache management
2. **Phase 2:** Build GPU shader pipeline (PIXI.Filter + GLSL fragment shader)
3. **Phase 3:** Temporal blend for smooth conquest animation
4. **Phase 4:** Config/UI cleanup, verification

### Key Rules
- Use `git ac "message"` for commits
- Never use `&&` in PowerShell
- PIXI v8.15.0 — `PIXI.Filter`, `PIXI.GlProgram`
- Max 64 stars, 8 players in uniform arrays
- Test PIXI v8 Filter API before writing full shader

## Work Log

### Phase A: Shared Module + PV2 Refactor
- `597476e` — Created `territoryFeatures.ts` with `VirtualSite` type, `computeCorridorVirtuals()`, `computeDisconnectVirtuals()`
- `66b76a0` — Refactored `PowerVoronoiRenderer.ts` to use shared module (no-duplicate-implementations rule)

### Phase B: Same-Owner Border Merge
- `d6fd797` — Track `enemyInfluence`/`enemyOwner` separately from overall second-star. Borders only render when closest enemy (different owner) is nearby.

### Phase C+D: Corridor + Disconnect Virtual Sites
- `53e9162` — Pack virtual sites after real stars in data texture. Corridor sites get player's index, disconnect sites get `DISCONNECT_PLAYER_INDEX (254)`. Shader skips rendering for owner 254.

### Phase E: Single Blended Border
- `dee652b` — Look up enemy owner's color, blend 50/50 at boundary, brighten for contrast. Skip disconnect borders.

### Territory Mesh Offset & Coverage
- `0595be4` — Expand mesh by 10% beyond padding
- `7e77ca3` — **Root cause identified:** mesh geometry was cached once on first call with stale dimensions. Added `cachedMeshWorldW/H` tracking + rebuild when dimensions change.
- `3bd82a0` — **Edge fade asymmetry fixed:** fade computed from `(0,0)` but stars start at `(minX, minY)`. Added `uContentMinX/Y` uniforms computed from actual star bounding box. **User confirmed fix works ✅**

### Border Width Consistency
- `3bd82a0` — **Borders had irregular thickness.** Normalized border distance by `fwidth(bestInfluence - enemyInfluence)` to get constant screen-space width. **User confirmed fix works ✅**

### Zoom-on-Cursor Fix
- `e7c67ee` — `handleWheel` used `GAME_WIDTH`-based centering but `applyZoomTransform` used content-centered reference. Aligned them. Not from territory commits. **User confirmed fix works ✅**

### UI Sliders
- `3bd82a0` — Added corridor toggle + spacing, disconnect toggle + distance sliders to DF settings
- `38387d2` — Added Expansion (0%–50%) and Smoothing (0–100) sliders

### Junction Smoothing
- `38387d2` — Added `secondInfluence` tracking (any owner) for junction detection. alpha-blends at 3-way junctions.
- `cd6b04f` — **Bug fix:** smoothing was fading same-owner junctions too (creating gaps). Switched to `enemyInfluence` (different owner only). **User confirmed gaps gone.**

### Expansion Cache
- `cd6b04f` — Track `cachedMeshExpansion` so mesh rebuilds when slider changes.

## Status at End of Session

### ✅ Working
- Same-owner border merge
- Single blended border (50/50 owner colors)
- Constant-width borders (fwidth normalization)
- Symmetric edge fade (content min bounds)
- Zoom-on-cursor
- Corridor virtual sites (toggle + spacing slider)
- Expansion slider (with cache invalidation)
- Smoothing slider (enemy-only junction fade)
- Morph/transition animation

### ⚠️ Needs Investigation
- **Disconnect Buffer toggle** — UI appears but user reports it "does not work." Code looks correct: `computeDisconnectVirtuals()` generates sites, config fingerprint includes toggle, shader skips owner 254. May need to test if any virtuals are actually generated given current map topology.
- **Corner rounding quality** — Smoothing (alpha fade at enemy junctions) creates soft corners but differs from PVV2's Chaikin polygon smoothing. User may want something closer to PVV2.
- **PVV2 feature parity** — User noted corridors "worked better in PVV2." May need tuning or algorithmic differences.
