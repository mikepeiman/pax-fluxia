# UI Work — 2026-03-02

## MainMenu References (requested)

| File | Lines | What |
|------|-------|------|
| `src/lib/components/ui/MainMenu.svelte` | Full component | Primary main menu |
| `src/lib/components/ui/MainMenu refactor.svelte` | Full component | Refactor variant (unused) |
| `src/lib/components/ui/menuDefs.ts` | Data constants | Pure data extracted from MainMenu |
| `src/lib/components/game/GameContainer.svelte` | L6 (import), L281 (render) | Imports and renders MainMenu |
| `src/lib/stores/multiplayerStore.svelte.ts` | L353, L402 | Legacy aliases + auto-detect MP |
| `src/lib/components/ui/_archived/MultiplayerLobby.svelte` | L5, L23, L115 | Shares localStorage keys with MainMenu |

**Summary**: MainMenu is imported and rendered in `GameContainer.svelte`. It uses `menuDefs.ts` for data constants. A refactor variant exists but is not wired up. MP lobby shares localStorage keys.

---

## Session 2026-03-02 — Completed Fixes

### ✅ Map Centering Rewrite
- Root cause: world bounds started at (0,0) but stars had significant minX/minY offset → asymmetric dead space
- Fix: Track actual content bounding box (`contentMinX/Y`, `contentWidth/Height`)
- All centering/scaling/panning now uses content center point
- Yellow debug rect shows actual content bounds

### ✅ Star-Cycle Navigation
- Traced full player ID pipeline — IDs match correctly (`human-player` throughout)
- Filtering `s.ownerId === localPlayerId` works
- Added on-screen debug readout (visible on mobile when canvas debug on)

### ✅ Landscape Settings Scroll
- Added `overflow-y: auto`, `height: 100vh/dvh`, full styling to landscape `.area-controls`

### ✅ Mobile Viewport Height
- Added `100dvh` fallback on `.app-container` and `.game-layout`

### ✅ Canvas Debug Infrastructure
- Yellow content bounds rect + magenta crosshair
- `log.canvas()` category with in-game toggle
- Red border on `.area-canvas`

---

## Open Bugs

### 🐛 Pixel Renderer — Shows Nothing
- Pixel territory mode renders blank on mobile
- Needs investigation — may be resolution/DPI related

### 🐛 Lane Renderer — Broken Territory Display
- Lanes don't accurately capture territory boundaries
- Shown in screenshots — territories are oversized and misaligned
- May be related to content bounds rewrite (uses GAME_WIDTH/HEIGHT legacy values)

### 🐛 Portrait Mode — Wasted Space
- Bottom padding wastes vertical space
- Map generation doesn't maximize viewport utilization
- Related to "Stretch Map To Fit" feature below

---

## New Feature Ideas

### 🆕 Stretch Map To Fit (Toggle)
- Rerender map (with all UI hitpoints, lanes, locations) to fit viewport size
- Maximize visual space usage in both portrait and landscape
- Should apply to both random and premade maps
- Add as toggleable option in settings

### 🆕 Smooth Transitions for Star-Cycle & Center-Fit
- Current behavior: instant jump → perceptual discontinuity
- Need: animated transition (lerp/ease) for both starcycle and fitcenter actions
- Smooth camera pan + zoom animation

### 🆕 Main Menu Full Redesign
- Current menu wastes space on both desktop and mobile
- Needs complete rework