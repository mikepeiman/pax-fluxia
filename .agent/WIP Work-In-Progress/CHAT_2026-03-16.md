# Chat Log 2026-03-16

## 12:00
> User wants to wire UI dropdown selections to the actual rendering pipeline. Style dropdown should control which renderer runs. Geometry should flow from FG2 canonical data.

## 12:20
> User noted the Territory Engine has a render stage — said this is antithetical to intent. Territory Engine should be geometry data only, not possess a renderer.

## 12:35
> User showed screenshots of the pre-existing three-concern UI (Style, Fill Transition, Border Transition). Noted two rendering systems overlaid (angular + voronoi).

## 12:38
> User: "Data Engine" should be renamed "Geometry", made first subsection, contain dropdown + sliders. Need "None" options on all three render selects.

## 12:41
> User emphasized: Territory Engine should NOT have a render stage. It's the geometry data generation layer.

## 13:02
> User asked about "Shape / Motion" section — Distance Metric (Length/Hops) appears obsolete. Trace it.

## 13:06
> User: Morph Speed should be renamed "Conquest Animation Timing". Morph Control Points should be kept for future use. Name explicitly what should be removed.

## 13:10
> User confirmed: remove Distance Metric and Fill Mode. Keep Morph Control Points. Also noted PVV2, PVV3, and canonical Territory Render modes missing from Style dropdown. #idea

## 13:16
> User showed screenshot from yesterday's state with Territory Engine enabled, DY4 Dynamic mode. Needs that available as a Style option and whatever DY4 was producing.

## 13:49
> User reports both modes produce same malformed render. Full logs show PVV3 running even with Power Voronoi selected. Missing mapping warnings for territoryFillTransition and territoryBorderTransition.

## 13:55
> User provided diagnostic log results. Routing works for territory_engine, vs_pvv3, power_voronoi. Bug: "none" resolves to territory_engine due to backward compat.

## 13:58
> User confirms all modes switch correctly now. Territory Engine (DY4) still visually broken but wants to tackle architecture refactor first before fixing that render mode.

## 18:12
> User: "You've lost a bit of context, just reanchor to rules" — requested re-read of .agent/AGENT.md.

## 18:14
> User: "I hate to distract you, but we need deferred orders back, they were broken/regressed dozens of commits ago. Please insert into your plan queue." #bug B-43 — deferred orders broadly broken, needs git archaeology to find regression commit.

## 18:41
> User: "DY4 has not rendered correctly for awhile, a number of commits. I do not think it is sacrosanct anymore. Include it for full refactoring, if anything needs to be done." #design D-81 — DY4 de-sacrosanct, include in refactor scope.

> User: "We will have to recover its functionality after the refactor as it is/was our strongest VFX result." — DY4 recovery is high priority post-refactor. B-44 opened.


