# Chat Log 2026-04-09

## 09:00
> Good morning. First, follow protocol for today's session and chat logs.
> Remove all BMAD items and references from all of yesterday's documentation audit.

## Later (doc-audit remediation)
> User reported BMAD rows still present in `MARKDOWN_MASTER_INDEX.tsv` (screenshot), asked for removal, CSV instead of TSV, files under `implementation-plans/2026-04-08/`, dedupe. **Done:** `doc-audit/` canonical tree; `(?i)bmad` exclusion; CSV + updated links; process duplicates removed from working tree. **After commit:** re-run both generators so `git ls-tree HEAD` matches disk.
