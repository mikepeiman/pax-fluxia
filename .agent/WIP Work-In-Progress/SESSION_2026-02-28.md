# Session 2026-02-28

## Focus: Mobile Gameplay Polish, Ideation, & UI/UX Foundations

### Commits

| Hash | Description |
|------|-------------|
| `2941ec9` | feat: mobile UX polish, territory per-renderer color controls, panel sync expansion, game balance tuning, PWA scaffolding |
| `d71e331` | fix(ui): remove BOM from GameSettingsPanel causing PostCSS build error; chore: update roadmap and rules |

---

### Today's Planned Work
- [x] Fix PostCSS build error from `GameSettingsPanel.svelte` BOM
- [x] Consolidate active and archived agent rules
- [x] Document and map new features (F-92 to F-97) to the project roadmap
- [x] Typography pass (F-97): Imported Rajdhani, Inter, JetBrains Mono and set app.css baselines
- [x] Compact Main Menu controls (F-94): Built custom `RangeDual` component for minimum/maximum links and optimized spacing
- [x] Mobile: hide all desktop UI (sidebar, settings columns) on mobile
- [ ] Verify on phone via LAN (`--host`)

### 1. Mobile UX Polish (recovered from crashed session)
- **Single-finger pan**: touch on empty space (no star hit-test) enters pan mode — no long-press required
- **Simplified mobile menu**: replaced full icon ribbon with a single ☰ button (top-right, 44×44px circle)
- **Speed controls center-bottom**: `.overlay-bottom-left` repositioned via `left:50%; transform:translateX(-50%)` on mobile breakpoint
- **Compact SpeedControls**: responsive breakpoint shrinks buttons/gaps at ≤1024px
- **Removed pull-to-refresh hardening**: `touchmove` listener dropped (CSS `overscroll-behavior: none` handles it)
- **Removed SVG starfield**: entire `<svg class="starfield-bg">` removed from GameContainer
- **Removed duplicate mobile actions section**: Restart/Audio/Quit buttons dropped from drawer

### 2. Territory Per-Renderer Color Controls
- **Exclusive toggling**: `selectTerritory()` — enabling one renderer disables all others
- **Per-renderer Saturation/Lightness**: added for Graph, Voronoi, Pixel, Metaball (previously shared)
- **Graph Pattern controls**: dropdown (none/stripes/crosshatch/dots) + Scale + Rotation sliders
- **Wider slider ranges**: pressure (0→5), metaball radius (→800), threshold (→2.0), strength (→20), edge fade (→20)

### 3. Panel Sync Expansion
- **`panelDefaultsFromConfig()`**: bootstraps panel defaults from GAME_CONFIG values (no more undefined keys)
- **~50 new `PANEL_CONFIG_MAP` entries**: graph territory, pixel extras, connections, star ring/glow, density, orbit, surge, animation, transfer, selection hex
- **Settings labels renamed**: "Ship Appearance" → "Stars & Ships", "Path & Easing" → "Ship travel"
- **CategoryThemeBar fix**: action buttons `padding: 0 10px; min-width: 30px; flex-shrink: 0`

### 4. Game Balance Tuning
- **Timing**: BASE_TICK_MS 1200→1050, ANIMATION_SPEED_MS 1200→1050, themes normalized to 1150ms
- **Transfer**: TRANSFER_RATE 0.07→0.1, STARTING_SHIPS 70→40
- **Combat**: DAMAGE_PER_SHIP 0.105→0.075, LETHALITY 0.35→0.1, CONQUEST_THRESHOLD 25→12
- **Conquest**: CONQUEST_TRANSFER_PERCENTAGE 60→30, COLOR_DELAY_TICKS zeroed
- **Retreat/scatter**: RETREAT_CAPTURE_RATE 0.1→0.25, SCATTER_CAPTURE_RATE 0.2→0.4
- **Ships**: SHIP_GLOW_INTENSITY→0.3, DEPART_MODE nearside, DEPART_JITTER_MS 175, LANE_OFFSET_PX 25
- **Visual**: STAR_ICON_SCALE 0.55→0.8, ORBIT_BASE_RADIUS 5→9, SHIP_BASE_SIZE 3→3.3, BG cleared

### 5. PWA Scaffolding
- manifest.json link + apple-touch-icon in app.html
- Service worker registration (`/service-worker.js`)
- `apple-mobile-web-app-capable`, `theme-color` meta tags

### Deferred Work
- Mobile phone testing via LAN (`bun run dev -- --host`)
- Tauri Android build exploration
