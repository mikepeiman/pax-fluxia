# Session 2026-03-07

## Territory Pipeline Refactor + Straight-Line Borders (Bridge Migration)

### Completed Steps
- Step 1 complete (`c8a5b7c`): Added WIP plan document for this refactor.
- Step 2 complete (`09b8aeb`): Moved DF panel writes to canonical `setSetting` flow; fixed DF mapping drift in `settingsDefs.ts`.
- Step 3 complete (`c1618c5`): Added DX write telemetry + diagnostics payload and deterministic rebuild trigger keyed to relevant UI writes.
- Step 4 complete (`f0f9d54`): Replaced Chaikin/marching-wobble vector smoothing path with straight-line regularization over ownership boundary graph.
- Step 5 complete (`03ab2f7`): Added border-family framework (`straight`, `curved`, `segmented`), with straight implemented and deterministic stubs for others.
- Step 6 complete (this session): Removed direct `GAME_CONFIG` writes in territory control handlers and finalized WIP + feature status updates.

### Final Behavior (Current)
- Territory settings in `ControlsSection-Territory.svelte` now mutate runtime state through `updatePanel(...)` path only.
- DX virtual-site changes are now instrumented and can trigger deterministic rebuilds after UI writes (prevents drag-state vs steady-state drift).
- Straight-line vector border mode now regularizes sampled boundary paths into piecewise straight world-space segments with round joins/caps and optional softness halo.
- New `DF_BORDER_FAMILY` setting is wired end-to-end, defaulting to `straight`.

### Known Follow-Ups
- `curved` and `segmented` border families are currently stubs that intentionally fall back to straight rendering.
- DX behavior still needs user visual verification against target gameplay expectations.
- Border quality/performance tradeoff needs user-side playtest validation on target hardware and zoom levels.


## 2026-03-07 16:25:59 - DF vector border perf chunking
- Reverted callsite DF throttling regression (flicker/no persistent territory) via commit a0baab6.
- Implemented chunked vector border ownership-grid rebuild in DistanceFieldTerritoryRenderer to avoid long single-frame stalls.
- Added adaptive rebuild cadence and site-pruning path to reduce sampleOwnerFromSites cost.
- Verified touched-file diagnostics remain clean under bun run check filtered output.

