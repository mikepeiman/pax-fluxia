# Session Notes — 2026-03-03

## Kickstart Context

**Primary task**: Implement F-138 (Voronoi Merged Territories)

### Key Files
- `MergedVoronoiRenderer.ts` — boilerplate (duplicate of VoronoiRenderer, has TODO markers)
- `VoronoiRenderer.ts` — working reference (per-star Voronoi cells, d3-delaunay)
- `game.config.ts` — territory config keys
- `ControlsSection-Territory.svelte` — territory settings UI
- `settingsDefs.ts` — PANEL_CONFIG_MAP for slider persistence
- `.atlas/FEATURE_STATUS.md` — feature tracker

### What Was Decided (D-34)
- Marching squares (Contour renderer, F-104) shelved — geometry too noisy for junction correction
- Voronoi (d3-delaunay) is the foundation: clean polygons, natural ~120° junctions, proper topology
- `d3-delaunay` v6.0.4 already installed

### F-138 Implementation Plan
1. **Merge same-owner cells**: remove shared internal edges, chain remaining boundary edges into unified polygons per owner-cluster
2. **Minimum star boundary margin (F-139)**: push boundary vertices within 5× orbit radii of any star center outward
3. **Bézier arc smoothing**: at junction vertices (where 3+ owners meet), quadratic Bézier through the junction — much cleaner on Voronoi geometry
4. **Chaikin smoothing**: optional, on final merged boundary

### Key Rules
- Use `git ac "message"` for add+commit (never separate commands)
- 10X slider rule: provide 10× "reasonable" range for new FX sliders
- Never use `&&` in PowerShell — run commands separately
- Follow slider reactivity pattern: add entries to `PANEL_CONFIG_MAP`
- Use `log.sys()` etc., never raw `console.log`

### Open Bugs
- B-28: Lane territory renderer broken
- B-29: Portrait mode wastes vertical/horizontal space

## Work Log

### F-138 / F-144: Territory Rendering — Full Day

1. **Power Voronoi V2** — multiple fixes to animated territory transitions (F-143), strength-blended shared borders (F-142). This is currently the best-performing renderer.

2. **Distance Field Territory Renderer V1 (F-144)** — implemented from scratch:
   - Multi-source Dijkstra on star/lane graph
   - Lane projection rasterization into texture
   - Animated borders via distance lerp
   - Full HSLA controls, configurable borders, hops/length metric
   - **Result: 0 FPS** — CPU rasterization of ~460K pixels/frame was fundamentally too slow
   - Attempted fixes: morphological smoothing (majority-vote), Chamfer distance transform — still 0fps
   - **V1 shelved**

3. **Post-mortem analysis** — compared what we built vs the [Deep Technical Guidance](../F-138-ModifiedVoronoi/Deep%20technical%20guidance.md):
   - Guidance prescribed GPU pipeline (PIXI.Filter/Shader + RenderTexture)
   - We built CPU-only pipeline — the exact scenario guidance warned against
   - Created detailed V2 implementation plan following guidance exactly

4. **Documentation produced:**
   - `6th-approach.md` — distance field approach description
   - `APPROACH_COMPARISON.md` — comparison of all territory rendering approaches
   - Updated `FEATURE_STATUS.md` — F-144 V1 shelved, V2 planned
   - Detailed implementation plan for F-144 V2 (GPU pipeline)

### Commits
- `git ac "New rendering documentation produced"` — 8 files, 3181 insertions

