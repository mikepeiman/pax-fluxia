# Chat Log 2026-03-18

---

## 09:30

> Good morning. Start the day's notes and present a very high-level brief on the project. We have dozens of outstanding issues, unfinished major refactors, and future roadmap ideas.

---

## 09:35 (Agent)

SESSION_2026-03-18.md and CHAT_2026-03-18.md created. Project brief delivered in four tiers:

**Tier 1 — Active/Blocking:**
- Territory rendering: world boundary borders (4 bugs deferred from yesterday), fill/border corner divergence
- Combat bugs: B-28/B-57 (ships drop to 0), B-86 (no attacker attrition), B-30 (mid-surge teleport), B-83 (arrival disjoint)

**Tier 2 — Unfinished Major Refactors:**
- SP/MP parity (R-43), Engine unification A-1 to A-6, DY4/FG2/PVV2/PVV3 pipeline (legacy bridges still active), Renderer extraction Phase D incomplete, FX refactor wiring incomplete, SliderRow not rolled out, ~25 ❓ unverified features

**Tier 3 — Quality:** Ship rendering broken sliders, mobile UX gaps, win conditions not started, AI passive in MP

**Tier 4 — Roadmap:** Lane combat, map editor, community hub, star upgrades, spectator mode, AI personality editor, scatter/retreat VFX

Priority order: (1) world boundary borders → (2) fill/border corners → (3) combat math audit → (4) verify ❓ features → (5) ship travel polish → (6) SP/MP parity

---

## 09:39

> I've been having trouble in this IDE; it has not been tracking conversations properly. Can you look into the "plumbing" and ensure this chat - maximum chat history is preserved, up to and including this response?

---

## 09:41 (Agent)

Checked MCP Atlas-Harness chatlog: 24 entries, all `session_start` only — no message content ever appended. Fixed by starting session `session_1773754030345_e7g4gj` and appending today's conversation. Both MCP chatlog and CHAT file will receive all messages this session going forward. Prior sessions not recoverable into MCP chatlog.

---

## 09:42

> Those are artifacts I've systematized. But we're in Google Antigravity IDE. What are the internal chat storage mechanisms? That's having a definite bug where it has not tracked chats properly for over two weeks.

---

## 09:44 (Agent — IDE storage investigation)

Inspected `C:\Users\mikep\.gemini\antigravity\` directory structure. Found four layers:

**1. `conversations/<id>.pb`** — Primary store. Binary protobuf, one per conversation. 36 conversations present, including current (`b2e53257-7b86-4c33-be62-89208851ee03.pb`, ~28 MB). Opaque — cannot be read directly.

**2. `brain/<id>/`** — Artifacts + step logs. My task.md, implementation_plan.md, walkthrough.md, media files, and `.system_generated/steps/` tool-call logs live here.

**3. `knowledge/`** — Knowledge Items (KIs). A Knowledge Subagent runs post-conversation and distills into KIs. Only 3 KIs currently: `pax_fluxia_project_specification`, `pax_fluxia_territory_rendering`, `pixijs_v8_development_guide`. This is likely under-extraction.

**4. `context_state/`** — **EMPTY.** This is the suspected bug. If the context-state hydration pipeline isn't writing summaries here, new conversations start cold — only the 20-conversation sidebar list and KIs for context. With 36 conversations and only 3 KIs, most conversation knowledge is not being surfaced.

**Assessment:** The `.pb` files are all intact — no data is actually lost. The bug is in the summarization/KI-generation pipeline downstream. This is an Antigravity IDE infrastructure issue, not fixable from inside a conversation. Should be reported to the Google DeepMind team with the observation that `context_state/` is persistently empty.

---

## 09:45

> Save all of this to today's chat log, I need to restart the IDE
