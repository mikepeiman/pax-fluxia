# Session 2026-03-20

## Focus: Fix territory transition splice detection

### Commits
| Hash | Description |
|------|-------------|
| `068561b` | fix: circular span matching in splice detection, whole-ring interpolation fallback, border rendering from morph geometry |
| `1e8818d` | refactor: pivot transition detection from span-based to proximity-based (REVERTED — user corrected this approach) |
| `47c2b10` | refactor: implement 2-pass splice planner with explicit RingTransitionKind, invariant checks, and structured diagnostics |

### 1. Fix Transition Splice Detection (initial attempt)
**Root cause hypothesis**: `findRingSpliceWindow` linear walk fails because spans are in different order.
**Fix attempted**: Circular span matching. Result: partially worked but didn't solve core issue.
**Correction by user**: The logs showed CLASSIFICATION BUGS, not algorithm failure. prefix=5/5 producing 0% stationary is an internal contradiction in the planner state, not proof that span-based splice is wrong.

### 2. 2-Pass Splice Planner (correct approach)
**Design**: Topology finds the neighborhood; geometry resolves stationarity within it.
- Pass 1 (topological): `findRingSpliceWindowTopological` — cyclic span matching → candidate changed window in SPAN space
- Pass 2 (geometric): `refineSpliceWindowGeometrically` — point-level displacement comparison → exact boundaries
- Classification: `classifyRingTransitionKind` — 5 explicit kinds (unchanged, splice-replace, splice-insert, splice-delete, fallback-snap)
- Invariant checks: `staticCount + changedCount ≈ totalSamples`, no `patch=0` for splice kinds

Key files: `types.ts`, `findRingSpliceWindowTopological.ts`, `refineSpliceWindowGeometrically.ts`, `classifyRingTransitionKind.ts`, `createTerritoryTransitionPlan.ts`, `sampleTransitionFrame.ts`

### Deferred Work
- User visual verification of transition rendering
- Removal of `PolygonMorphTransitionHandler` legacy fallback
- FX system integration for territory transitions
