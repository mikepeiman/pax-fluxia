# Session 2026-03-19

## Focus: Registry unification (Phase 6) + Transition layer FX wiring (Phase 7)

### Commits
| Hash | Description |
|------|-------------|
| `d0ce8da` | refactor: Phase 6 registry unification - merge static/dynamic into unified TerritoryMethodId |
| `3acdc7d` | fix: replace last selection.mode gates in fg2SeedGraph.ts with methodId |
| `1618d07` | feat: Phase 7 - territory transition FX handler with PVV2 wiring |

### 1. Phase 6: Registry Unification
Merged separate static/dynamic territory method registries into a unified system. `TerritoryMethodId` replaces `TerritoryStaticMethodId` + `TerritoryDynamicMethodId`. Single `TERRITORY_METHODS[]` array with `TERRITORY_METHOD_BY_ID` lookup. DY4 SACROSANCT preserved.

Key files: `types.ts`, `registry.ts`, `engine.ts`, `game.config.ts`, `index.ts`, `ControlsSection-Territory.svelte`, `fg2SeedGraph.ts`

### 2. Phase 7: Transition Layer FX Integration
Created `territoryTransitionHandler.ts` — an `FXHandler<ConquestEvent>` at priority 200 that records pending territory transitions in a `TerritoryTransitionState` singleton. Registered in FX `defaults.ts`. Wired PVV2 renderer to consume transitions.

Key files: `territoryTransitionHandler.ts` (NEW), `defaults.ts`, `PowerVoronoiRenderer.ts`

### Deferred Work
- Remove ~30 unused CSS selectors in ControlsSection-Territory.svelte
- Remove backward-compat type aliases after full validation
