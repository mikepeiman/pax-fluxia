# Session 2026-03-15

## Focus: Territory Engine V3 Master Plan + Phase 0 Audit

### Commits
| Hash | Description |
|------|-------------|
| `ffefc44` | Territory Engine V3 Master Plan + decisions D-72 through D-74 |
| `358c055` | Rename docs to new naming convention |
| (pending) | D-75 lane-exclusivity, D-76, B-44 PVV3 border regression, MECHANICS §13 |

### 1. V3 Master Plan Writing
Reviewed all V2 master plan documents, D-67 decision, Perplexity frontier generation research, and architectural-transfer-precheck rule. User provided extensive feedback that fundamentally reframed the architecture: the five modes should be render/animation modes, not data generation strategies. One rock-solid data engine produces front geometry and ownership; render modes present it with distinct visual styles. "Backend" term retired. D-70 vocabulary adopted.

Wrote analysis report (two iterations with user review), then the full V3 master plan:
- Two-layer architecture: Data Engine + Render Modes
- Seven render modes: Vector Stroke (priority), Distance Field Glow, Pressure Wave, Pixel Art/Retro, Terrain Shader, Metaball/Organic, No Animation
- Phased implementation starting with Vector Stroke unified pipeline validation
- Documented decisions D-72 through D-74 in DECISIONS.md

### 2. Phase 0: PVV3Renderer Audit
Audited PVV3Renderer.ts (2095 lines, 65 functions). Key findings:
- The FG2 canonical path (lines 1637-1907) already operates as a unified pipeline
- Per-frame morph animations are **disabled** — territories snap on rebuild
- ~600 lines of legacy fallback code is dead when FG2 produces data
- The data engine call (`runFG2DataPipeline()`) lives *inside* the renderer function (line 1642) — architectural inversion that V3 Phase 1 should fix

### 3. User Feedback Corrections
- **D-75**: Lane-exclusivity constraint replaces DX. Only 1-2 player holdings allowed to underlay any lane. No 3rd player territory touches a lane. Added to DECISIONS.md, MECHANICS.md §13, and V3 master plan (architecture diagram, constraint table, glossary).
- **D-76**: Current active territory mode is FG1/PVV2, not PVV3/FG2 (context for agent, not for docs)
- **B-44**: PVV3 border regression — borders only render for one fleeting frame when settings change. Root cause: GameCanvas blanket-hides all voronoiContainer children every frame (line 1094-1097), but PVV3 only re-shows `borderGraphics` inside the full rebuild path (after early-return at line 1460). `fillGraphics` is re-shown before the check (line 1439), `borderGraphics` is not.
- **Settings persistence**: Verified still working — GAME_CONFIG Proxy debounce-saves to localStorage on every property change. User's saved settings may differ from code defaults.

Key files modified:
- `.atlas/DECISIONS.md` (D-75, D-76)
- `.atlas/MECHANICS.md` (§13 Territory Lane Constraints)
- `.atlas/FEATURE_STATUS.md` (B-44)
- `.agent/WIP Work-In-Progress/permanent-references/territory/2026-03-15__1435 territory_engine_master_plan_v3.md` (DX→lane-exclusivity corrections)

### Deferred Work
- Phase 1: Separate data engine call from renderer (fix the inversion)
- Fix B-44 PVV3 border regression if PVV3 becomes active renderer
- Re-enable transition animation with proper clear-and-redraw-all-per-frame
- Remove ~950 lines of legacy dead code from PVV3Renderer

