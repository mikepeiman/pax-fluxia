# Session 2026-03-15

## Focus: PVV3 Phase 3 Geometry Extraction + V3 Territory Architecture Phase 0+1

### Commits
| Hash | Description |
|------|-------------|
| `53a83e3` | refactor(PVV3): extract geometry into borderPipeline, frontierLoops, morphUtils, mergeUtils (-1068 lines) |
| `06d996c` | ui: remove border smooth slider from game options, hardcode to 0 |
| `23e74b8` | fix: wire PVV3 to FG2 canonical data — single source of truth for fills + borders |
| `e2233f1` | feat: add CanonicalTerritoryData + RenderMode interface (V3 Phase 1) |
| `07ae3f3` | docs: add D-78, D-79 decisions for V3 Phase 0+1 |
| `489da45` | ui: restructure Territory panel for V3 — remove Active Layers toggles + mode picker |
| `747027c` | feat: V3.1 three-concern architecture — Style + Fill + Border dropdowns, -2657 lines |

### 1. Phase 3: PVV3 Geometry Extraction
Extracted 1,068 lines of geometry utility functions from `PVV3Renderer.ts` (1869→800 lines) into four new modules under `geometry/`.
Key files: `PVV3Renderer.ts`, `geometry/borderPipeline.ts`, `geometry/frontierLoops.ts`, `geometry/morphUtils.ts`, `geometry/mergeUtils.ts`

### 2. Border Smooth UI Removal
Removed "Border Smooth" slider from `ControlsSection-Territory.svelte`. Set `VORONOI_BORDER_SMOOTH` default to `0` in `game.config.ts`.

### 3. V3 Phase 0: Wire PVV3 to FG2 Canonical Data
Root cause: `legacy_pvv3` adapter called `renderPVV3()` without artifacts. Fix: call `runFG2DataPipeline()` first, pass artifacts. PVV3 now always uses single-source FG2 data.
Key files: `engine.ts` (2 lines added)

### 4. V3 Phase 1: CanonicalTerritoryData + RenderMode Interface
Created `renderMode.ts` with typed `CanonicalTerritoryData` interface and `RenderMode` contract. Added `extractCanonicalData()` to `engine.ts`. Updated `PVV3Renderer.ts` to accept typed data (removed 15 lines of inline casts). Fixed `GameCanvas.svelte` callsite. Updated barrel exports.
Key files: `renderMode.ts` (new), `engine.ts`, `PVV3Renderer.ts`, `GameCanvas.svelte`, `index.ts`

### Deferred Work
- Phase 2: Mode Selector UI
- Remove PVV3 legacy datagen dead code
