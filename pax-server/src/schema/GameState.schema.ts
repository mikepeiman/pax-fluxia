// ============================================================================
// Re-export schema from @pax/common
// ============================================================================
// This file provides backward compatibility for existing imports.
// The authoritative schema definitions are now in @pax/common.

export {
    PlayerSchema,
    StarSchema,
    ConnectionSchema,
    PointSchema,
    MapMeasurementSchema,
    TerritorySchema,
    GameRoomState,
    MapSchema,
    ArraySchema
} from "@pax/common";
