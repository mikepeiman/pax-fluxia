# Chat Log 2026-03-15

## 18:15
> implement a star data pill that displays star information in a compact, customizable format. Horizontal pill layout: starNumber | activeShips / damagedShips. Adding comprehensive styling controls in-game settings, including layout, padding, gap, background opacity, font family, size, scale, leash line visibility, ownership-colored border and fill.

## 19:45
> I do NOT want a leash, at least not gray. Another option toggle: Data tag color: `Universal | Player` → HSLA selector for universal. Data tag border width.

## 20:16
> Nice. But no effect toggle "inline". I prefer clear Toggle Button-style, larger buttons `Inline | Stacked`. I do NOT want a leash, at least not gray. Another option toggle: Data tag color: `Universal | Player` → HSLA selector for universal. Data tag border width.

> NEXT, after:
> 1. MP lobby: it is built in, fully shown in main menu. All human players in waiting room and all in lobby. Lobby host can assign slots to AI or humans; open and waiting until human players fill them.
> 2. Map selection is identical for SP and MP. User selects Random or Classic. Chooses settings, and whatever those settings, either Start Game for SP or Create Lobby for MP. However, lobby host CAN change map or settings after lobby exists but before game starts.

## 20:25
> Document all of today's design work and dialogue. Especially features and intent.
>
> UI design for Main Menu concept:
> - We will have numbered steps to set up a game:
>   1. left column, dark bg, rounded corners, 1rem margin left: Choose A Map Type
>     - sub-type: `Classic | Custom | New`
>     - each type shows a list of map cards with accurate thumbnails
>     - `New` type is random, offers customization features to auto-generate a map
>     - `Custom` type is any saved game that is not Classic. Created from scratch, saved from random, or edited classic
>   2. next column: Choose your opponents
>     - SP: select number of players and AI settings, or load an AI Theme
>     - MP: Multiplayer lobby, shows all available lobbies and create lobby option
>   3. next column: Mode & Options
>   4. Start — big inviting button
> - MP lobby built into main menu, host assigns AI/human slots, waits for humans
> - Map selection identical for SP and MP, host can change after lobby creation

## 20:26
> I do not want this implemented destructively. Current main menu is worth keeping, though it needs refinement. I want options.

## 20:26
> So you'll somehow make this an option for me to explore. Maybe a sticky topnav toggle. #idea
